L3-2j ITEM (4) -- CREDENTIAL GATE AND EGRESS AUDIT
recorded 2026-08-24

== WHAT WAS AUTHORIZED ==
provider   : Google
model      : gemini-3.1-pro-preview
credential : GEMINI_API_KEY
scope      : Google Gemini API authentication for the L3-2j item (4) cross-provider revalidation ONLY

== PRESENCE CHECK -- BY PRESENCE AND LENGTH CLASS ONLY ==
No value was read into a printable form, echoed, hashed, logged, persisted, or partially revealed.
Only counts and a length CLASS were produced.

  GEMINI_API_KEY  name present in this shell's environment : 1
  length class                                             : PLAUSIBLE_>=32
                                                             (a class, not a length, not a prefix)

The probe was validated against variables known to be present BEFORE its positive result was
trusted -- the same discipline the L3-2j negative probe used, applied in the other direction:

  OPENAI_API_KEY  : 1        PATH : 1        HOME : 1        GEMINI_MODEL : 1
  GEMINI_MODEL_ID : 0  (unset -- the shim therefore uses its pinned default)

  GEMINI_MODEL is exported by the operator's shell as 'gemini-3.1-flash-lite-preview', which is NOT
  the authorized model. It was NOT used and NOT substituted. The transport shim reads
  GEMINI_MODEL_ID, which the run script sets explicitly to 'gemini-3.1-pro-preview'.

== PROVIDER AUTHENTICATION -- GOOGLE'S OFFICIAL ENDPOINT ONLY ==
  GET https://generativelanguage.googleapis.com/v1beta/models   ->  HTTP 200
  models returned                                               : 50
  'models/gemini-3.1-pro-preview' present in the returned list  : YES

The probe carried the credential in the x-goog-api-key header and NOTHING ELSE -- no scenario text,
no evidence, no corpus content, no customer data.

== VERDICT ==
CREDENTIAL GATE PASSED. This is the second time the gate has passed (section 39.1 was the first);
it FAILED at section 31.1, section 38.1, section 38.8, L3-2i and L3-2j.

A PASSING GATE IS A MEASUREMENT OF A MOMENT, NEVER A STANDING PROPERTY. The key can be revoked,
rotated, rate-limited or removed. Re-probe by presence and provider response before relying on it.

A PREVIEW MODEL LABEL IS NOT A CONTENT DIGEST. 'gemini-3.1-pro-preview' can change under its label,
unlike qwen3-coder:30b at the pinned digest 06c1097efce0. This phase therefore ran an explicit
MODEL-DRIFT CONTROL (V_S_STRUCT against the frozen L3-2h rows) rather than assuming the label still
resolves to the same weights. See rootcause/driftcontrol-vs-frozen-l32h-gemini.json.

== EGRESS, MEASURED ==
destinations contacted by this phase : https://generativelanguage.googleapis.com  -- ONE
                                       (127.0.0.1:11435 is the LOCAL shim process, not a destination)
local ollama (127.0.0.1:11434) calls : 0   -- NO qwen inference was run by this phase
credential material read or emitted  : none
production or customer data sent     : none
sealed corpus bytes sent anywhere    : none
credential present in any artifact   : NO (verified by scan; see PRESERVATION_POST.txt)

HTTP requests to generativelanguage.googleapis.com, itemised:
  auth / metadata probe (GET /v1beta/models, credential only, zero content)          :   1
  transport smoke test  (synthetic 'Reply ok=YES', zero scenario content)            :   1
  inference (6 variants x 24 already-open diagnostic scenarios)                      : 144
  one HTTP 503 retried once by the shim's bounded transient-fault retry, then 200    :  +1
  ------------------------------------------------------------------------------------------
  TOTAL HTTP requests                                                                : 147
  TOTAL inference calls billed against the corpus                                    : 144

Transport integrity over the 145 shim-logged requests:
  status 200                : 144        status 503 (retried -> 200) : 1
  finishReason on every 200 : STOP       truncation (MAX_TOKENS)     : 0
  harness-level errors      : 0          scenarios lost              : 0
  latency mean 18.9 s, p90 19.7 s, max 271.0 s (the max is the provider-side slowdown that
  accompanied the 503; L3_OLLAMA_TIMEOUT_MS was 300000, so no call was aborted)
  thought tokens: n=144, mean 592, min 232, max 930, total 85,215 (thinkingLevel 'low', the floor)
  prompt tokens mean 2,354; output tokens mean 311

WHAT WAS TRANSMITTED. Only the 24 ALREADY-OPENED diagnostic scenarios -- development and
retired-holdout material -- parsed out of the locked harness and cross-checked field-by-field
against the frozen L3-2g artifact. NO SEALED SET WAS OPENED. The sealed corpus is hash-verified
unchanged before and after and appears in ZERO artifacts of this phase.
