# Offline reconnect

Not executed with real browser network controls. Draft persistence across reload, reconnect, interrupted sync, retry, duplicate prevention, stale writes, conflicts, authentication recovery, and data-loss behavior remain blockers.
