# Security regressions

No bypass was used. `DEV_AUTH_BYPASS=false` during browser verification. Production CORS remains exact-origin; only explicit loopback origins are enabled outside production. Full browser authorization and object-storage tests remain incomplete.
