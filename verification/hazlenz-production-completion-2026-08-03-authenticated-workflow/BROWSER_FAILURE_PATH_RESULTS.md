# Browser failure and authorization paths

Not completed. API-level entitlement isolation is passing, but browser-level free/expired users, unauthorized objects, stale sessions, rate limits, upload failures, report failures, and network interruption remain unverified.
