# EXPERT HAZLENZ — FINAL COHORT ASSEMBLY + GOVERNED SNAPSHOT (2026-08-31)

**Terminal: `FORMAL_COHORT_ASSEMBLY_BLOCKED — AWAITING_INDEPENDENT_SAFETY_REVIEW_AND_GOVERNED_EXTRACTION`.**

Not the success terminal. Phases 0–2 completed; **Phases 3–10 are blocked on two actions only you
can take**, and neither can be substituted by anything I could do here.

HEAD `37a5d1b5`, unmoved. **Provider calls 0. Production not accessed. Reserved material opened: 0.
Nothing committed, pushed, tagged or deployed.** No production file modified; no frozen artifact
modified.

---

## Phase 0 — freeze recheck: no drift

Twelve frozen artifacts re-hashed from the files themselves. **All twelve reproduce their sealed
values exactly.**

| artifact | sha256 | status |
|---|---|---|
| canonical constructor | `72dab946…8279` | unchanged |
| evaluation plan | `0b9b273a…2bd8` | unchanged |
| measurement contract | `022fb9db…f330` | unchanged |
| cohort contract | `c6d593be…b633` | unchanged |
| scorers | `bec26908…41cc` | unchanged |
| composition | `96f6c848…7323` | unchanged |
| prompt (v6) | `e02c15ea…f458` | unchanged |
| §119 projection | `f1cc7a61…e467` | unchanged |
| retirement registry | `cd1a114d…ef6c` | unchanged |
| augmentation corpus | `c744c939…32d9` | unchanged |
| cohort harness | `b4ea8b75…0fcd` | unchanged |
| construction policy | `9865e7d0…abcf` | unchanged |

`validate:negative-control-augmentation` re-executed: **29 passed, 0 failed**, and it re-derived the
three sealed augmentation hashes identically —
manifest `b2e96cc5…bf64`, truth keys `51afb054…b620`, provenance `e33fb8df…0339`.
`PROVIDER_INVOCATION_COUNT = 0`, `RESERVED_OFFSETS_OPENED = 0`.

**No semantic drift. The freeze holds.**

## Phase 1 — review packet prepared, verdict NOT obtained

`review/AUGMENTATION-REVIEW-PACKET.md` — `614e2dec…0a08`. Generated by
`backend/scripts/emit-augmentation-review-packet.ts` (`2d4547c3…2789`) **from the sealed corpus**,
so the packet cannot drift from the truth keys it asks you to bind.

All 16 rows, each with: row id, observation verbatim, location/task, present / defensible /
forbidden families, the defeating fact claimed for every forbidden determination, safe-or-resolved
state, clarification obligation, and any recorded interaction.

Withheld by construction, per the operation's own rule: no provider output, no gate score, no
threshold, no margin, no capability arithmetic, and no indication of whether any particular label is
load-bearing. A reviewer told which label a result depends on is no longer reviewing the source case.

### Verdicts returned — `INDEPENDENT_SAFETY_REVIEW_OBTAINED = TRUE`

The product owner returned final verdicts on all 16 rows: **13 APPROVE, 3 REJECT/CORRECT.** Recorded
verbatim in `negative-control-augmentation-review/HUMAN-REVIEW-RECORD.md` (`23cdf1fb…88a5`). The
packet they reviewed is unchanged at `614e2dec…0a08`.

| row | family | was | now | effect |
|---|---|---|---|---|
| `AUG-08` | `fall_protection` | FORBIDDEN | DEFENSIBLE | keeps `lockout_tagout` forbidden — **stays** a negative control |
| `AUG-13` | `electrical` | FORBIDDEN | DEFENSIBLE | only forbidden family — row **leaves** the population |
| `AUG-14` | `fall_protection` | FORBIDDEN | DEFENSIBLE | only forbidden family — row **leaves** the population |

Applied exactly as written. **Nothing was substituted, restored or added to recover the two lost
opportunities**, and no row was rewritten. `defensibleHazardFamilies` is derived, so each corrected
family lands in DEFENSIBLE by construction and the totality rule holds without hand-editing.

The `authoringRationale` on `AUG-13` and `AUG-14` still argues the defeat the reviewer overruled.
**Deliberately left as authored** — it records what the author believed, and the review record is the
authority that supersedes it. Editing authored reasoning after the fact would erase the very
disagreement the review exists to surface.

The corrected corpus is a different corpus and is recorded as
`FORMAL_EXPERT_NEGATIVE_CONTROL_AUGMENTATION_V2`. V1's sealed hashes still describe the pre-review
state; the §125 `SEAL.json` was preserved and restored byte-exact at `97bb5e67…b6d3`.

**Recomputed under the frozen contract — 29 passed, 0 failed:**

| | pre-review | post-review |
|---|---|---|
| rows with forbidden-family truth | 14 | **12** (floor 10) |
| available negative-control capability | 57 | **55** (≥ 48, margin **7**) |
| manifest | `b2e96cc5…bf64` | `b2e96cc5…bf64` (unchanged — no observation text moved) |
| truth keys | `51afb054…b620` | **`29a0c38e…9169`** |
| provenance | `e33fb8df…0339` | **`5f0b5338…f23e`** |

Post-review seal: `state/SEAL-V2-POSTREVIEW.json` (`480c7bdd…b0e8`).
Regression after the change: `expert-nocall-harness` 141/0, `expert-cohort-instrument` 155/0,
`expert-contract-foundation` 56/0. `RESERVED_MATERIAL_OPENED = FALSE`, `HOSTED_CALLS = 0`.

**One honest note on the anti-contamination gate.** The first post-correction run **failed D.2**,
because the review packet I generated quotes all 16 observations and I had filed it under a directory
name outside the scan's existing `negative-control-augmentation` path exclusion. The packet is
precisely the material that exclusion exists for — the corpus's own review document, created after
sealing, shown only to a human, never sent to a provider. I refiled it under
`negative-control-augmentation-review/` so the unmodified rule covers it. **The scorer was not
edited, relaxed or bypassed**, and the failure is recorded here rather than quietly resolved.

## Phase 2 — runbook verified, one real defect found and corrected

The v1 runbook (`…cohort-source-blockers-2026-08-31/governed/READ-ONLY-EXTRACTION-RUNBOOK.md`)
**would have failed at the server.** Its Step 3 selects `r.title`, `r."approvedText"` and
`r."backingState"` from `regulatory_release_records`. **None of those three columns exists** —
confirmed against both `regulatory-release-record.entity.ts` and the DDL in migration
`1800000012000-RegulatoryReleaseRecords.ts`. The table's columns are `citation`, `citationKey`,
`recordChecksum`, `reviewState`, `reviewStateReason`, `payload`, `agencyCode`, `standardId`, plus the
KG-3F approval digests. `title` and the approved text live inside the frozen `payload` jsonb, whose
shape is fixed by `normalizeStandardRecord()`. The v1 runbook flagged its own projection as "a best
guess" and asked you to improvise with `\d` on failure; you should not be debugging SQL against
production.

`governed/READ-ONLY-EXTRACTION-RUNBOOK-V2.md` — `f0f1fad2…2ddf`. v1 is left **unmodified** as
evidence of the operation that produced it.

Four changes:

1. **Step 3 corrected** to read `payload ->> 'title'` and `payload ->> 'canonicalText'`, derived from
   the entity and the normalizer rather than guessed.
2. **Step 0 added** — table-existence and migration-name check. The migrations are applied out of
   band (`start:render`, `synchronize: false`), so if `regulatory_release_records` was never migrated
   to production, Step 1's write probe fails with `42P01 undefined_table` instead of
   `25006 read_only_sql_transaction`. Both are errors, and it would be easy to read the failure as
   "the session refused the write" when **read-only was never demonstrated at all**. Step 0 removes
   that ambiguity. Migration state is judged by name, not by ledger row count.
3. **Step 4 added** — `reviewState` distribution. See below.
4. **Stale warning removed.** v1 closed by saying the cohort still could not be frozen because
   negative-control capability was 45 against 48. That blocker is closed: capability is 57.

Read-only enforcement is unchanged and remains server-side: `BEGIN TRANSACTION READ ONLY` plus
`PGOPTIONS=-c default_transaction_read_only=on`, with an explicit `WHERE false` write probe that must
be **refused with `25006`**. If the probe succeeds, the runbook stops.

**`GOVERNED_SNAPSHOT_OBTAINED = FALSE`.** The production credential is not in this environment and I
neither ask for nor handle it.

## What Step 4 is expected to show, stated before you run it

`GovernedStandardView.backingState` is **derived at request time, never stored**. Its only persisted
input is `reviewState`, which has three values: `unreviewed`, `mechanically_validated`,
`reviewer_approved`. Only the last can support an `APPROVED_*` backing state.

I expect **zero** `reviewer_approved` records, because KG-3A defect B deleted the only writer of that
state at finalization — the old derivation collapsed "this source may be fetched automatically" into
"a reviewer approved this record", and was correctly removed rather than re-pointed.

If that holds, every governed record the cohort supplies is honestly `UNAPPROVED_RECORD`. Checked
against the frozen contracts, that is **not** a failure:

- `M06` / `M07` key off `governedStandards.length > 0`, not approval. Unaffected.
- `M08` tests `EXPERT_CANNOT_APPROVE_AN_UNAPPROVED_RECORD`; unapproved records *create* that opportunity.
- No `REQUIRED_CLASS_MINIMUMS` entry requires an approved record.

It does mean **one axis of `M07` grounding will go unexercised** — no cohort row can supply a cleanly
approved governed record. That will be reported as a stated limitation, not absorbed into a green
result. If Step 4 returns a non-zero `reviewer_approved` count, the mapping changes and I will not
assume either way.

**Separate note on vocabulary, recorded not acted on.** The frozen cohort contract classifies
`DISAGREEMENT_OPPORTUNITY` by `g.backingState !== 'APPROVED'`, but `'APPROVED'` is not a member of the
production vocabulary (`APPROVED_EXACT`, `APPROVED_SECTION_ONLY`, `APPROVED_NO_TEXT`,
`UNAPPROVED_RECORD`, `NOT_IN_RELEASE`, `NO_ACTIVE_RELEASE`, `RESOLVER_UNAVAILABLE`). Assembly must
therefore define an explicit mapping from production backing states into the cohort's vocabulary.
**I have not chosen that mapping and have not touched the frozen contract.** It is a governance
decision with a measurable effect on `DISAGREEMENT_OPPORTUNITY`, and it belongs with the Phase 3
reconciliation once the real distribution is known.

## Phase 5 — minimum reserved opening, determined but NOT executed

The operation gates opening behind Phases 1 and 4, so nothing was opened. The determination, from
metadata only:

**The review changed this plan, exactly as the dependency predicted.**

Pre-review, offset 2 alone was sufficient with a margin of 1:

```
seed 14 + PopA 13 + augmentation 14 = 41 ; + GAUNTLET_OFFSET_2 (8) = 49  >= 48   margin 1
```

Post-review, with augmentation at 12, it is not:

```
seed 14 + PopA 13 + augmentation 12 = 39 ; + GAUNTLET_OFFSET_2 (8) = 47  <  48   INSUFFICIENT
                                           + GAUNTLET_OFFSET_3 (8) = 55  >= 48   margin 7
```

**Revised plan: `GAUNTLET_OFFSET_2` and `GAUNTLET_OFFSET_3` must BOTH be opened.** Two rejections
cost exactly the margin that made a single-offset opening possible. `REALISM_OFFSET_1` and
`REALISM_OFFSET_2` still stay sealed — they carry zero negative-control capability, and no frozen
composition requirement reaches for them.

This is why Phase 1 had to precede Phase 5, and it is not sequencing ceremony: had the reserve been
opened first, offset 3 would have been opened after the fact to cover a shortfall created by the
review — which is the shape of opening reserve to rescue a number. Neither offset is opened yet.

`analyze:reserved-cohort-sufficiency` re-run, content boundary verified, **no observation text read
or emitted**: `RESERVED_MATERIAL_OPENED = FALSE`. It also confirms the assembly cost — five truth-key
fields (`defensibleHazardFamilies`, `decisionCriticalGaps`, zero-owed rows, `recordedInteractions`,
`governedStandards`) have **no corresponding label in either reserved corpus**. Authoring them
requires reading each observation, which *is* opening the offset. Phase 6 is authoring work, not
mechanical selection.

## Phases 3, 4, 6, 7, 8, 9, 10 — not performed

Each depends on a governed snapshot that does not exist yet, on a human verdict not yet given, or on
both. Nothing was partially done and left in place.

```
FORMAL_COHORT_COMPOSITION_VALID   = NOT_EVALUATED
GOVERNED_RECORD_SUPPLIED          = 0     (>= 40 required)
FORMAL_COHORT_V1_FROZEN           = FALSE
P4_PROPOSAL_ISSUED                = FALSE
P4_PRESPEND_AUTHORIZATION         = FALSE
```

No P4 block is issued. Phase 10 is conditioned on a frozen cohort, and issuing a proposal with
recalculated spend against 60 request bodies that do not exist would be a fabricated number.

## State

```
INDEPENDENT_SAFETY_REVIEW_OBTAINED      = TRUE       (13 approve, 3 corrected)
GOVERNED_SNAPSHOT_OBTAINED              = FALSE      <-- sole remaining blocker
AVAILABLE_NEGATIVE_CONTROL_CAPABILITY   = 55         (post-review; >= 48, margin 7)
RESERVED_OFFSETS_OPENED                 = 0
PROVIDER_INVOCATION_COUNT               = 0
PRODUCTION_ACCESSED_BY_CLAUDE           = FALSE
FORMAL_COHORT_SPENT                     = FALSE
PLANNED_FUTURE_REPRESENTATION_DEBT      = TRUE       (untouched)
LOCAL_R2_EVIDENCE_CLIFF_DEBT_OPEN       = TRUE       (untouched)
```

Carried unchanged: `EVALUATION_CORPUS_POLICY.closed` under-reports three retirements (needs its own
authorization); `M02`/`M07`/`M09` denominators model-output-dependent; `P2_DETERMINISTIC_CONTROL =
ABSENT`; extraction pattern-based; `CONTRADICTS_DETERMINISTIC` not authoritative.

## Phases 3–8 — executed. Terminal B.

**Terminal: `EXPERT_HAZLENZ_FORMAL_COHORT_ASSEMBLY_BLOCKED — FORMAL_COHORT_SEMANTIC_TRUTH_UNAVAILABLE`.**

- **Phase 3 reconciliation: all checks PASS** — `governed/GOVERNED-SNAPSHOT-RECONCILIATION.md`,
  proof `proofs/governed-reconciliation.txt`. Release `federal-core-2026-08-28.1`, `active`, 64
  records, 64 distinct checksums and citations, manifest `680540d9…`, snapshot `a2c5dc32…`.
- **Backing state resolved from production truth**: all 64 `mechanically_validated` →
  `UNAPPROVED_RECORD`. No `APPROVED_*` invented. **The frozen contract is NOT defective** —
  `expert-cohort-composition.ts:144` names UNAPPROVED records as what exercises M08. M06/M07
  unaffected, grounding preserved, `TRUTH_LEAK = 0`.
- **Limitation preserved**: `REVIEWER_APPROVED_GOVERNED_RECORDS = 0`,
  `APPROVED_RECORD_GROUNDING_EXERCISED = FALSE`. Not a failed gate; not to be implied as covered.
- **Blocker, measured**: `CLARIFICATION_OWED` 3/20 and `CROSS_HAZARD_INTERACTION` 5/10, and
  **opening offsets 2+3 changes neither** — that artifact carries no gap and no interaction label.
  Reserve left intact. `COHORT-ASSEMBLY-BLOCKER.md`, proof
  `proofs/max-achievable-composition.txt`.
- **Regression**: 56 / 51 / 131 / 141 / 58 / 88 / 40 / 155, augmentation 29/0, reconciliation 13/0,
  `tsc` exit 0. All nine frozen artifacts re-verified unchanged after all work.
- **Docs updated**: blueprint decision row **D-138**; `INSITE_CURRENT_STATE.json` checkpoint,
  readiness, `nextSlice` and a new `expertFormalCohort` block.

## Worktree

HEAD `37a5d1b5`, 1 ahead of `origin/main`, unmoved. Two untracked paths added, both non-production:
`backend/scripts/emit-augmentation-review-packet.ts` and this verification directory. All prior
uncommitted work preserved; nothing reset, stashed, discarded or reverted. Prompt v6, `analysis.v2`,
the §119 projection, `toExpertFamily`, M01–M17 thresholds, scorer semantics, grounding, quote
binding, normalization and customer HazLenz behaviour are all untouched.
