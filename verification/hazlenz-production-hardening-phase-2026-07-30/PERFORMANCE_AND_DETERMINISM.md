# Performance and determinism

The guided adapter remains a single deterministic controller-boundary projection with a SHA-256 input hash and no wall-clock fields. Contract tests confirm stable hashes. It performs no database queries and bounds additional standards, questions, and finding candidates.

No production-path benchmark was completed in this phase. Repeated hydration/query profiling and re-analysis race cancellation remain future work.

