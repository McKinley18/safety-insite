# Mobile visual audit

The shared header’s oversized negatively positioned logo caused clipping. It now uses a bounded responsive container and clipped internal scaling. Main content retains explicit mobile bottom safe space.

Final visual inspection at 320×844 dark shows:

- complete, readable brand mark
- no horizontal overflow
- visible progress steps
- readable cards/inputs
- visible focus ring
- bottom navigation below content

The 390×844 light workflow also passed. Tablet and desktop screenshots were not captured in this phase.

