L3-2j — CREDENTIAL GATE AND EGRESS AUDIT
recorded 2026-08-24T11:57:49Z

== WHAT WAS AUTHORIZED ==
provider   : Google
model      : gemini-3.1-pro-preview
credential : GEMINI_API_KEY
scope      : Google Gemini API authentication for L3-2j item (4) ONLY

== PRESENCE CHECK — BY PRESENCE ONLY, NO VALUE READ, PRINTED, HASHED OR PERSISTED ==
method: the NAME 'GEMINI_API_KEY=' was counted in the environment of this shell and of every
        ancestor process, using 'grep -o NAME= | wc -l'. Only counts were ever produced. The
        value was never read into a variable, never echoed, never hashed and never written.

shell environment                  : GEMINI_API_KEY present = 0
ancestor pid=51712   claude                       : GEMINI_API_KEY present = 0
ancestor pid=19907   -zsh                         : GEMINI_API_KEY present = 0
ancestor pid=19906   login                        : GEMINI_API_KEY present = 0
ancestor pid=23114   /System/Applications/Utilities/Terminal.app/Contents/MacOS/Terminal : GEMINI_API_KEY present = 0

== THE METHOD WAS VALIDATED BEFORE IT WAS TRUSTED ==
A negative result from an env probe is worthless unless the probe is known to work. The same
probe was run for variables that ARE present, on the same processes:
  OPENAI_API_KEY     in the Claude Code process env : 1
  GEMINI_MODEL       in the Claude Code process env : 1
  PATH               in the Claude Code process env : 1
  HOME               in the Claude Code process env : 1
  GEMINI_API_KEY     in the Claude Code process env : 0

  GEMINI_MODEL is exported by ~/.zshrc as 'gemini-3.1-flash-lite-preview', which is NOT the
  authorized model, and it carries no credential. It is named here only because a reader who
  greps for GEMINI will find it and must not mistake it for the authorization.

== VERDICT ==
GEMINI_API_KEY IS NOT PRESENT in this session's environment or in any ancestor process.
The task statement said it had been exported into the environment that launched this session.
It had not been. This is stated as a fact about the measured environment, not as a judgement
about the instruction.

CONSEQUENCE: L3-2j item (4) — cross-provider revalidation of the F-CL-01/B10 proof on
gemini-3.1-pro-preview — IS NOT EXECUTED. The provider axis remains n = 1. The blocker is
unchanged from 31.1 -> 38.1 -> 38.8 -> 39.1 -> 40, and L3-2j adds nothing to it and removes
nothing from it.

NOT DONE, deliberately, rather than worked around:
  - no credential was sought from any file, keychain, config or history;
  - no hosted-provider call was attempted with an absent or substituted credential;
  - GEMINI_MODEL's flash-lite value was NOT substituted for the authorized pro model;
  - no result was estimated, extrapolated or simulated for the second provider.

== EGRESS, MEASURED ==
destinations contacted by this phase : http://127.0.0.1:11434  (local ollama) — ONE, and local
hosted-provider calls                : 0
credential material read or emitted  : none
production or customer data sent     : none
sealed corpus bytes sent anywhere    : none

local inference calls made by this phase:
  shipped-pipeline corpus runs : 8 variants x 24 scenarios = 192
  locked-harness re-derivation : 3 variants x 24 scenarios =  72
  TOTAL                        : 264 local calls, 0 hosted
