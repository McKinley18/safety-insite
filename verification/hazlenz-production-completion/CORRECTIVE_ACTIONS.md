# Corrective actions

Corrective actions and tasks now consume the server-issued urgency policy for priority and due date. Multi-hazard candidates create distinct actions rather than a single blended action.

The prior hierarchy-of-controls implementation remains intact. This pass did not claim broader corrective-action intelligence because the authentic suite still reports mechanism/evidence failures.

