# Production files changed

- `frontend-next/components/inspection/SafeScopeInspectionStep.tsx`: render each API-provided multi-hazard decomposition separately with mechanism fragment, confidence, and evidence-gap context. This fixes a material UI omission discovered in real Chromium.
