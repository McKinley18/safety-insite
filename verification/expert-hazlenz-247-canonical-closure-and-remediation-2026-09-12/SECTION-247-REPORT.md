# §247 — Canonical Expert Architecture Closure and Bounded Remediation: Final Report

Zero provider calls. Zero database operations. No commit, no push, no tag, no deploy.

## Terminal

    EXPERT_HAZLENZ_BOUNDED_SUCCESSOR_REMEDIATION_IMPLEMENTED —
    DRIVER_ROLE_HOSTED_CONFIRMATION_AUTHORIZATION_REQUIRED

Bounded remediation is implemented, local regression passes at 1,458 assertions with zero failures,
and the hosted confirmation instrument is frozen and unexecuted.

## Required report fields

| Field | Value |
|---|---|
| Canonical Expert production path | `backend/src/safescope-v2/expert-hazlenz/expert-hazlenz-analysis.ts` |
| Legacy independent Expert route | **CLOSED** |
| `anthropic-expert-provider.ts` | **ADAPTER** — delegates to the canonical contract and envelope |
| test-218 relocation failures | **0** (113 / 113 pass) |
| Promoted semantic source | **UNCHANGED** — 39 relocated modules, 26 byte-identical, 13 import-path-only, 0 other |
| Driver-role representation | **IMPLEMENTED** — `roleJustification` on every basis entry, §247 additive successor |
| K6 | **6 admissible / 0 inadmissible** |
| Provider schema compatibility | **PASS** — `anyOf` + `const` verified through the real adapter pipeline |
| Strict schema parity | **PASS** |
| Decision-complete property review | **PASS** |
| Decision-complete evidence review | **PASS** |
| G3 mechanism | **PREVENTED** |
| C6 mechanism | **PREVENTED** |
| C2 mechanism | **PREVENTED** |
| C5 mechanism | **EXPOSED** |
| M8 mechanism | **PREVENTED** (both sub-mechanisms) |
| G5 mechanism | **REMAINS** |
| M2 / K6 | **PREVENTED** |
| Candidate Identity v2 | **17 / 17**, all mechanically resolved, 0 declarations used |
| Production build | **PASS** — 1 error, the frozen known one, no additional |
| Frozen TypeScript provenance errors | expected known set only |
| Protected semantic regressions | **0** |
| Provider calls | 0 |
| Database operations | 0 |
| Hosted confirmation instrument | **FROZEN** |
| Hosted confirmation calls | 0 |
| Commit / push / tag / deploy | NONE |

## The historical mechanisms, stated honestly

Three of the seven are prevented structurally, one is exposed, one remains, and the review-artifact
three are closed by the new surfaces.

- **M2 / K6 — PREVENTED.** The union makes the pair unwritable. Replay of the 30 preserved artefacts
  found exactly one inexpressible basis entry across 70, and it is M2's.
- **M8 — PREVENTED**, both ways. Its three controls drivers with no per-driver link now fail
  `CONTROLS_DRIVER_WITHOUT_DISCHARGING_CONTROL`, and a storm-cell fact labelled manufactured cannot
  hold a controlling role at all.
- **C5 — EXPOSED, not prevented.** A cessation driver must now name the alongside control it
  considered and say why it is insufficient. C5 would have had to name the banksman and argue it
  inadequate. **Whether that would have changed the answer is unknowable from frozen output**, and
  nothing here claims it would.
- **G5 — REMAINS.** A hold-to-run fact the model labels decision-critical is still admitted. This is
  the §244 preregistered expectation, written before implementation and deliberately not revised
  after it.
- **G3, C6, C2 — PREVENTED.** The adjacent established fact, the open sibling, and an evidence review
  artifact that states approval cannot settle, all now on the product surface.

§243 is not rescored. Its result stands at D HOLD RELEASE.

## What replay can and cannot show

30 preserved artefacts replayed, 70 basis entries examined. It established K6 structural handling,
contract compatibility, and that 8 cessation drivers and 17 controls drivers would now owe a
justification they do not carry. **It cannot show that the model would choose better roles.** The
preserved bytes predate the field; a schema change is not behavioural evidence. Only the frozen
hosted confirmation can answer that, and it has not been run.

## Verification actually executed

Fourteen suites, **1,458 assertions, 0 failures**:

| Suite | Result |
|---|---|
| `test-239-contract-binding-closure` | 336 / 0 |
| `test-237-posture-closure` | 204 / 0 |
| `test-235-posture-stabilization` | 209 / 0 |
| `test-expert-nocall-harness` | 141 / 0 |
| `test-expert-provider-failure` | 131 / 0 |
| `test-218-structured-property-verifier` | 113 / 113 PASS |
| `test-expert-projection-equivalence` | 90 / 0 |
| `test-220-kr1-property-authority` | 43 / 43 PASS |
| `test-246-productionization` | 33 / 0 |
| `test-247-driver-role-and-k6` | 33 / 0 |
| `test-224-declaration-capability` | 33 / 0 |
| `test-247-decision-complete-review` | 31 / 0 |
| `test-226-property-selection-capability` | 31 / 0 |
| `test-expert-anthropic-adapter-repair` | 30 / 0 |

The production build reports exactly one error, the frozen `POSTURE_REF_KINDS_237` annotation, which
was preserved and not repaired. **No additional TypeScript error exists.**

## Two judgements worth flagging

**Closure A was delegation, not reinterpretation.** The authorization requires a stop if closing the
legacy route needed semantic reinterpretation. It did not, and the evidence is that both protected
suites asserting the adapter's behaviour pass unchanged — 30 of 30 and 90 of 90 — including the one
that compares behaviour through the real assembled request. The §239 contract is a byte-reversible
extension of the base contract, so pointing the adapter at a later link of one chain changed which
link it reads, not what any link means.

**test-218 was repaired without weakening.** All six pinned modules had moved byte-identically in
§246, so their frozen expected digests still hold; only the path they are read from changed. Not one
expected digest was edited or relaxed, and C15 still requires all three of its conjuncts.

## The frozen hosted confirmation

Six fresh cases, one call each, under USD 1.00. No §243 observation is reused. Six preregistered
gates: zero unsafe under-restriction, zero manufactured or negated condition given a cessation
driver, zero K6-invalid output, 6/6 structural admission, **role-presence coherence ≥ 5 of 6 against
§243's 0.60**, and 6/6 justification completeness. All six must hold. The threshold is frozen and may
not be lowered after results are seen.

This is the final bounded representational attempt at the driver-role mechanism. If it fails, the
return is `EXPERT_HAZLENZ_DRIVER_ROLE_CAPABILITY_LIMIT_REMAINS` and a product-owner capability
decision — not another taxonomy, sidecar, verifier, prompt layer or schema layer.

## Working-tree changes, by section

**Pre-existing, untouched.** Nine tracked modifications under `backend/src/` and one under
`backend/scripts/`, plus a large body of untracked scripts, all present before §246. None was
modified by §246 or §247.

**§246.** `contract/` with 39 relocated modules; `expert-hazlenz-analysis.ts`;
`expert-request-envelope.ts`; `expert-semantic-transport.ts`; `test-246-productionization.ts`; 39
shims under `scripts/lib/`; the §246 verification package.

**§247.** Modified: `anthropic-expert-provider.ts` (delegation, config moved out),
`expert-request-envelope.ts` (now owns the configuration), `property-authority.ts` (four additive
packet fields), `settlement-review.ts` (new evidence review artifact),
`test-218-structured-property-verifier.ts` (path resolver), `test-246-productionization.ts`
(strengthened for successors). Added: `decision-review-model.ts`,
`contract/expert-247-posture-contract.ts`, `contract/expert-247-role-justification-projection.ts`,
`test-247-driver-role-and-k6.ts`, `test-247-decision-complete-review.ts`,
`replay-247-historical.ts`, and this verification package.

Every file §247 modified was already untracked in the working tree; no tracked file was changed by
this section. Pre-change digests of all five modified files were captured before editing.

## What remains

The hosted confirmation is frozen and needs authorization to execute. Whole-product reacceptance was
not designed, as instructed. `tsconfig.scripts-239.json` and `tsconfig.scripts-240.json` still scope
by glob over `scripts/lib` and match shims; they encode frozen TypeScript provenance this
authorization preserved, so repointing them belongs to a section authorized to touch it.

§243 stands at D HOLD RELEASE. No successor candidate has been frozen.

STOP.
