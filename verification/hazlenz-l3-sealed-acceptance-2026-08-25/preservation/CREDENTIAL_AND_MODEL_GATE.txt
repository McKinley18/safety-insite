L3 FINAL SINGLE-USE SEALED ACCEPTANCE -- PHASE 2 CREDENTIAL AND MODEL GATE
recorded 2026-08-25T20:49:44Z
HOLDOUT_SPENT AT THIS MOMENT = FALSE

  ANTHROPIC_API_KEY_PRESENT = TRUE

  inherited environment, [ -n ... ]     PRESENT
  variable NAME declared, ${VAR+set}     YES
  env(1) name-anchored match COUNT      1
  negative control (undeclared name)    NOT DETECTED   -- the check still discriminates

  HANDLING: presence only. The value was NOT printed, logged, hashed, persisted, copied into
  any artifact, measured for length, or shown by prefix or suffix. No branch of any check
  above can emit it. It appears in ZERO artifacts of this package. The frozen shim sends it
  only as the x-api-key header and never logs it.

  Claude Code's own claude.ai / subscription authentication: NEITHER INSPECTED NOR USED (s.47.7).
  No disposable readiness call was made by this phase. D-93 (s.60) is the immediately preceding
  callability and identity evidence: 1 call, HTTP 200, requested and returned claude-sonnet-5,
  PROVIDER_CALLABILITY / MODEL_IDENTITY / EXECUTION_PATH_COMPATIBILITY all PASS.

  FROZEN EXECUTION CONFIGURATION REQUIRED AND CONFIRMED:
    provider   Anthropic     (api.anthropic.com/v1/messages via shim 76d3e039)
    model      claude-sonnet-5
    NO FALLBACK MODEL AND NO FALLBACK PROVIDER IS PERMITTED OR IMPLEMENTED.
    The runner asserts the configured model string equals 'claude-sonnet-5' and THROWS
    otherwise, before any row is read.

  CONTRACTUAL PRECONDITION, NOT VERIFIABLE HERE: the key must belong to an organization under
  the Anthropic Commercial Terms (D-79). Presence does not prove it and this phase does not
  claim it.

  Phase 2 PASSES. The gate continues to Phase 3.
