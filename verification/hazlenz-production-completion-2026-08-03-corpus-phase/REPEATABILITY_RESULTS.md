# Repeatability

The existing authenticated 20-case suite passed after the entitlement fix. A repeatability sample across the generated corpus was not accepted because the full corpus execution was incomplete. No nondeterminism claim is made.

