# PROMOTING THE DETERMINISTIC→EXPERT PROJECTION ONTO THE PERMANENT PATH — 2026-08-31

**Terminal: `EXPERT_HAZLENZ_DETERMINISTIC_PROJECTION_PRODUCTION_PATH_ACCEPTED — FORMAL_EVALUATION_COHORT_AUTHORIZATION_REQUIRED`** (Terminal A)

Zero hosted calls. 2 local Ollama calls (non-authoritative integration smoke). **$0.00.**
Nothing committed, pushed, tagged or deployed. **No customer activation.**

```
HEAD 37a5d1b5 (unchanged) · origin/main de655d2f (unmoved) · ahead 1/behind 0
stashes 4 (unchanged) · tags 24 (unchanged)

EXPERT_PROMPT_VERSION                   = hazlenz.expert.prompt.v6  (UNCHANGED)
EXPERT_ANALYSIS_CONTRACT_VERSION        = hazlenz.expert.analysis.v2 (UNCHANGED)
EXPERT_HAZLENZ_PROVIDER_VALIDATED       = FALSE
EXPERT_HAZLENZ_CUSTOMER_ACTIVE          = FALSE
17_MEASURE_EVALUATION_COHORT_AUTHORIZED = FALSE
LOCAL_R2_EVIDENCE_CLIFF_DEBT_OPEN       = TRUE
```

---

## The headline

**All seven §118 projected-arm cases produce a byte-identical provider request through the permanent
path, and every request byte-count matches the §118 preflight measurement of the request actually
sent to Anthropic.** `SEMANTIC_REQUEST_EQUIVALENCE = 100%`, with **no differences to justify** —
so §118's hosted evidence transfers to the permanent implementation.

| case | byte-identical | permanent chars | §118 measured chars |
|---|---|---|---|
| `R6` | **YES** | 24,897 | **24,897** |
| `V7` | **YES** | 24,864 | **24,864** |
| `R6-I` | **YES** | 25,391 | **25,391** |
| `V1-CTRL` | **YES** | 24,829 | **24,829** |
| `R6-H` | **YES** | 24,931 | **24,931** |
| `V8` | **YES** | 25,055 | **25,055** |
| `V5` | **YES** | 24,873 | **24,873** |

New protected suite `test:expert-projection-equivalence` — **88 assertions, 0 failed.**

## What changed — 3 production files + 1 line

| file | change |
|---|---|
| `expert-contract.types.ts` | `DETERMINISTIC_DISPOSITIONS`, `DeterministicControllingFact`, `DeterministicFamilyDisposition`, and one **optional** `deterministicFamilyDispositions?` field |
| `expert-deterministic-projection.ts` **(new)** | the prototype promoted unchanged in meaning — a **pure projection** over decisions the deterministic layer already produced |
| `expert-prompt.ts` | `renderDeterministicDispositionBlock`, byte-identical to the §118-confirmed text, appended with the same separator |
| `package.json` | +1 line: the equivalence gate script |

**No second applicability engine and no state reconstruction inside Expert.** The projection
evaluates no predicate, reads no observation text and reaches no applicability conclusion; its
parameter is structural, so it carries no dependency on the evidence foundation.

**`EXPERT_CONDITION_STATES` was not touched** — the foundation suite asserts it byte-identical to
the Level-3 vocabulary read as data, so `NOT_APPLICABLE` may not be forced into it. The disposition
vocabulary is separate and describes what the *deterministic* layer said.

**Prompt semantic version: UNCHANGED (`v6`). Permanent input projection: NOW POPULATED.**
`EXPERT_SYSTEM_PROMPT` is byte-unchanged (gate `F.2`). A previously-discarded piece of the
deterministic *result* is now rendered into the per-request input, beside the observation, the
findings and the governed records. This is not a behavioural prompt revision.

## R6 permanent-path proof (gates C.1–C.9)

```
machine_guarding = NOT_APPLICABLE @ 0.96
  provenance      : DERIVED_FROM_PRODUCTION_ENGINE       ← from the repaired §117 engine
  controlling fact: moving or accessible energy = CONTRADICTED
  rationale       : "…affirmatively contradicts 'moving or accessible energy'…"
```

Rendered under the header *"These families were EVALUATED by the deterministic engine. This is
different from a family it never considered."*

## Projection absence (gates D.1–D.6)

| input | rendering |
|---|---|
| `undefined` | **no section** — byte-identical to a pre-§119 request |
| `[]` | **no section** — an evaluation that produced nothing says nothing |
| populated | the assessment section, explicitly marked EVALUATED |

`D.3`: **an absent projection never synthesises a `NOT_APPLICABLE` state.**

## Anti-rubber-stamp, structural (gates E.*)

`V7` → `ACTIVE@0.96` (no false exclusion) · `R6-I` / `R6-H` → `UNKNOWN@0.45` (not excluded) ·
`V5` → `UNKNOWN` (can carry a clarification) · `V8` → `chemical_exposure` **absent**, still addable ·
`V1-CTRL` → `CONTROLLED@0.94` rendered with the override licence verbatim.

**No boundary suppression exists** — gate `F.1` asserts mechanically that
`expert-normalization.ts` does not mention the field, so no code path can drop an Expert candidate
because of a projected disposition. Deterministic state is context, not authority.

## Deliberately NOT promoted

The state-aware scorer stays in `scripts/lib/`. The authorization permits promoting it **only if it
belongs in the permanent evaluation harness** — and that harness does not exist. Promoting it would
be speculative production surface, and this phase is implementation of an accepted design, not new
design. **No pass/fail threshold changed; no historical score rewritten;
`expert-routing-metrics.ts` byte-unchanged.**

## Local integration diagnostic — `NON_AUTHORITATIVE_LOCAL_INTEGRATION_DIAGNOSTIC`

2 local Ollama calls end-to-end through the permanent path (provider → normalizer):
`R6` → `VALID`, `NOTHING_TO_ADD`, 0 candidates, 0 issues; `V7` → `VALID`, `ANALYZED`, 1 candidate,
2 clarifications, 0 issues. Confirms no gross integration mistake. **Not** used to supplement or
override §118's hosted evidence.

## Regression

Expert **56/58/40/30/51/131/141** + new **88/0** gate · quarantine **61/0**, **37/0** ·
deterministic corpus **16/16, 0 dangerous false negatives** · **0 dangerous / 0 life-critical
omissions** · **Population-A precision 100.0%** · 0 forbidden emissions · grounding,
Anthropic-adapter, standards/applicability suites all 0 · `tsc` 0 · **no new failing suite**.

**`PRE_EXISTING_BASELINE_FAILURE_UNCHANGED`** — identical counts to §118, **not passing**, not
investigated: `hazlenz-field-gauntlet` (92) · `hazlenz-authentic-gauntlet` (92) ·
`hazlenz-authentic-reasoning` (21) · `hazlenz-clarification-gauntlet` ·
`hazlenz-independent-standards-audit` · `safescope` · `safescope-standards` ·
`standards-corpus-integrity` (1).

## Confinement

Three production-capable files + one `package.json` line. Zero controller/service/module, auth,
billing, report/PDF, frontend, migration, governed-release or database involvement.
**`runExpertAnalysis` still has zero callers in `src/` — the layer remains off the customer path.**
`expert-normalization.ts`, `expert-routing-metrics.ts`, the Anthropic adapter,
`evidence-foundation.ts`, `shared-evidence-facts.ts` and `routing-fixtures.ts` are byte-unchanged.

## Retained limitations

- `P2_DETERMINISTIC_CONTROL = ABSENT`; §118's primary was **n = 3 per arm**.
- The deterministic extraction layer remains **pattern-based**; §117 does not prove universal
  linguistic coverage, and no vocabulary was expanded here.
- `CONTRADICTS_DETERMINISTIC` remains imperfectly calibrated and is **not** authoritative; the
  derived/adjudicated classification stays the meaningful diagnostic.
- The formal cohort harness does not exist, and Expert is still not customer-reachable.

## Next operation

**A product-owner decision on whether to authorize the formal 17-measure evaluation cohort.** The
blocker §118 named is closed. Two things belong in that decision: the cohort harness must be built
(including whether the recording instrumentation is promoted with it), and Expert remains OFF the
customer path with zero callers — so cohort evaluation would measure a layer that is still not
customer-reachable.
