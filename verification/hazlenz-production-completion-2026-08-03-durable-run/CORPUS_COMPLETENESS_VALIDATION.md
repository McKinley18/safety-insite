# Corpus completeness

The completed checkpoint contains exactly 129 unique IDs, with no duplicate IDs, no terminal transport failures, and no missing cases. Each case reached the authenticated endpoint. The full per-case output and source corpus remain in the continuation evidence directory.

