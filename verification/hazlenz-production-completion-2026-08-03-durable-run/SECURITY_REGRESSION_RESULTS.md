# Security regression results

Authenticated entitlement boundary passed; `DEV_AUTH_BYPASS=false`; free/expired denial, active access, cross-user isolation, concurrent requests, and sequential requests passed. The production throttler remained enabled throughout the corpus run.

