# Security regression results

Prior entitlement boundary and cross-user isolation suites passed. `DEV_AUTH_BYPASS=false` was used for the authenticated repeatability run. No secrets were written to artifacts. Live-provider authorization could not be tested without credentials.
