# Offline architecture

The inspection route exposes an Offline navigation state and local inspection wiring exists in the frontend. Real network-disconnect, queue, reconnect, conflict, quota, and expired-session behavior was not completed in this run.

