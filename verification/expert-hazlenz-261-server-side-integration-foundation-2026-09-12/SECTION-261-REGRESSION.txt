§261 BUILD AND REGRESSION
==========================
Baseline (before any §261 edit):
  npm run build          exit 0
  npm run build:render   exit 0

After §261 implementation:
  npm run build          exit 0
  npm run build:render   exit 0

Backend suites, run against disposable database test_insite_261_reg_201247
with the server booted on port 4232 against that same disposable database:

  test:persisted-decomposition-findings   exit 0
  test:finding-scoped-reviews             exit 0
  test:canonical-workflow                 exit 0
      25 scenarios, 3 analyses persisted, 4 cross-user denials,
      massAssignmentRejected true
  test:knowledge-release-provenance       exit 1  -- ENVIRONMENTAL, NOT A §261 REGRESSION

  The provenance suite reaches POST /inspections/:id/reports and receives 500 from
  CanonicalReportsService.generate -> StorageService.store -> S3PrivateStorageProvider,
  which throws "STORAGE_S3_BUCKET is required."

  Established, not inferred:
    - STORAGE_S3_BUCKET is absent from backend/.env (0 occurrences); no STORAGE_* is set.
    - The throw is in src/storage/storage-provider.ts, reached from src/reports/.
    - §261 changed 0 files under src/storage and 0 files under src/reports
      (git status --porcelain over both paths returns 0 lines).
    - §261 explicitly prohibits configuring object storage, so the gap was not closed.
  The suite's analysis-persistence phase completed; it failed only at report generation.

Nest application boot against the §261 schema: successful, all routes mapped,
no new route exposed.
