# §187A — Preregistration (human-readable mirror)

Written before the first provider call. The authoritative machine copy is ;
this file is a mirror and adds nothing.

- Cohort: HR-01, HR-04, HR-06, HR-08, HR-09 (REQUIRED only). SILENCE rows NOT rerun.
- Two-component path: one real first-pass Expert execution per row generates the verifier stimulus; three verifier-v3 executions per row run against that SAME frozen stimulus.
- Exactly ONE product-owner-reviewed unresolved owed fact per verifier execution, derived through the existing §184/§185 fixture and projection path.
- Model: claude-sonnet-5  ·  first-pass prompt hazlenz.expert.prompt.v15
- Caps: 20 planned calls, 24 hard, $2.00 hard.  Retries: 0.
- Strict semantic results are NOT computed by the harness. They are human judgements and were left to adjudication.

§179 is preserved as HISTORICAL_FIRST_PASS_SILENCE_EVIDENCE (SILENCE_PASS 12/15) and is never a contemporaneous control, never a paired comparator, and never combined into one accuracy score with §187A.

