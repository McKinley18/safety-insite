# Entitlement operations report

Platform administrators can issue time-bounded `pilot` or `support` grants with target, reason, tier, actor, and maximum 90-day expiry. Test grants remain available only through the pre-existing production-impossible disposable command.

Grant/revoke writes security audit events. Ordinary-user escalation returned 403. Revocation persisted `revoked` and immediate expiry. Existing expiry enforcement remains in `EntitlementService`.

Tests: 4/4 (ordinary denial, audited assignment, audited revocation, persisted state).
