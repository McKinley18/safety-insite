{
  "artifact": "SAFETY_INSITE_SECTION_301_BI4_ENTITLEMENT_RESOLUTION_REPAIR",
  "section": "301",
  "generated": "2026-09-15T21:26:47Z",
  "providerCalls": 0,
  "expertAnalyses": 0,
  "spendUsd": 0.0,
  "migrations": 0,
  "rootCause": {
    "confirmedExecutably": "normalizeStripeSubscriptionStatus was exercised over undefined, null, empty string, whitespace, garbage and every valid status. It returns a NON-EMPTY STRING in all cases — its final line is `return \"none\"`.",
    "proximate": "AuthService.resolveSessionContext called billingService.getBillingStatus with { userId, email, planCode, type } and WITHHELD subscriptionStatus, although the promo registration deliberately writes `subscriptionStatus: 'active'` onto the user row beside `planCode: 'pro'`. With no status supplied the resolver read 'none' and resolveAccessTier('pro','none',null) returned 'free'.",
    "secondary": "The `|| (fallbackTier === 'free' ? 'none' : 'active')` alternative in getBillingStatus — evidently intended to treat a paid tier carrying no explicit status as active — IS DEAD FOR EVERY POSSIBLE INPUT, because the left operand is total.",
    "whyItPresentedAsItDid": "Registration answers from its own local variable and is therefore truthful at that instant; the discrepancy only appears at the NEXT request, when the session is rebuilt through the resolver. That is why §300 saw `planCode: \"pro\"` followed immediately by a free session."
  },
  "authorityModel": {
    "purchasedSubscription": "A UserSubscription row. AUTHORITATIVE IN BOTH DIRECTIONS whenever one exists — it supplies both tier and status and overrides the user row, so it can revoke as well as grant.",
    "pilotEntitlement": "An entitlement_grants row: source, tier, status, startsAt, endsAt, reason, issuedByUserId. Bounded and revocable. Applied as a FLOOR — it raises a free result to pro and never lowers anything.",
    "promotionalEntitlement": "A promoCode at registration matching EMPLOYER_PRO_PROMO_CODES, a SERVER ENVIRONMENT allowlist. Writes planCode and subscriptionStatus onto the user row.",
    "internalSyntheticEntitlement": "No distinct mechanism exists. Internal/synthetic testing uses either the promo path or a bounded entitlement_grant.",
    "planCode": "On the user row: REQUESTED/GRANTED PLAN INPUT, not authority. On the JWT: the already-RESOLVED decision, minted by the server.",
    "subscriptionStatus": "On the user row: input to the resolver, paired with planCode. Never authority on its own.",
    "entitlementGrant": "Server-authoritative, bounded, revocable.",
    "featureEntitlements": "hasEntitlement(tier, feature) over BILLING_PLAN_DEFINITIONS. fullSafeScope is pro-only. Derived from the resolved tier and from nothing else.",
    "requestedPlanVersusAuthorizedEntitlement": "dto.planCode and dto.selectedPlan are declared on RegisterDto and NEVER READ by AuthService. They are REQUESTS. The only expression that decides the account tier is `const planCode = employerProPromoApplied ? 'pro' : 'free'`."
  },
  "precedence": [
    "1. A live UserSubscription row, if present, supplies BOTH tier and status and outranks the user row in both directions — a promo tier cannot survive a subscription Stripe has ended.",
    "2. Otherwise the user row's planCode + subscriptionStatus, resolved through resolveAccessTier, which requires status active or trialing.",
    "3. An active bounded entitlement_grant is then applied as a FLOOR: free -> pro only.",
    "4. fullSafeScope follows the single resolved tier."
  ],
  "security": {
    "promoIsServerControlled": true,
    "evidence": "The allowlist comes from process.env.EMPLOYER_PRO_PROMO_CODES, which a registering user cannot write to. A supplied code that is not on the list is REFUSED with 400 BEFORE any write, so a guessing attempt leaves no account behind. An unset, empty, whitespace or commas-only value yields an empty allowlist that matches nothing.",
    "publicSelfEscalationPossibleBeforeRepair": false,
    "whyNot": "dto.planCode and dto.selectedPlan were already never read. The §300 defect made the promo path grant NOTHING; it never made public registration grant TOO MUCH.",
    "wouldTheObviousRepairHaveCreatedEscalation": false,
    "butOneObviousVariantWOULDHave": "Making the dead `|| (tier === 'free' ? 'none' : 'active')` branch REACHABLE. That reads as: any account whose TIER claims pro is treated as having an ACTIVE subscription even though nothing says so — an entitlement widening from a caller-influenced field, in the one function every feature gate consults. It was DELETED instead, which is provably behaviour-preserving.",
    "decision": "PROCEED. A trusted server-side condition exists and was proven unforgeable by the registering user, so §301's STOP criterion is not met."
  },
  "repair": [
    "auth.service.ts resolveSessionContext now passes user.subscriptionStatus into getBillingStatus.",
    "billing.service.ts: the dead `|| (fallbackTier === 'free' ? 'none' : 'active')` alternative is removed rather than made reachable."
  ],
  "canonicalPath": "authorization (server allowlist or grant) -> registration/account row -> fresh authentication -> resolveSessionContext -> getBillingStatus (one resolver) -> effectivePlanCode -> signed JWT -> EntitlementGuard/EntitlementService -> /hazlenz/classify and Expert eligibility. Expert uses the SAME EntitlementGuard with the SAME fullSafeScope key, so it cannot obtain broader authority from a different calculation.",
  "noMigration": true,
  "newDefect": "EN-2 — an already-issued token retains capability until it expires, because hasFeature returns on the JWT tier claim before reaching the grant lookup. Measured, not inferred: classify returned 201 on a pre-revocation token while a fresh login correctly resolved free and was refused 402. Retention, not escalation; window bounded by JWT_EXPIRES_IN (default 15m). Registered, not repaired.",
  "tests": {
    "unit": "test-301-entitlement-authority.ts — 42 checks, in hazlenz:test",
    "integration": "test-301-entitlement-integration.ts — 35 checks, in hazlenz:integration:test, against a disposable test_* database with DEV_AUTH_BYPASS refused",
    "durableAntiSelfPromotionGuard": "A SOURCE assertion that auth.service.ts never reads dto.planCode, dto.selectedPlan, dto.subscriptionStatus or dto.tier, that the account tier is decided by exactly one expression, and that its only non-free branch reads the server environment. A source assertion rather than a behavioural one because the property is 'no code path reads a caller-supplied plan into the account', and a behavioural test can only sample the paths someone thought to try."
  },
  "section300Reproduction": {
    "before": "the pre-repair expression, applied to the very account row the suite had just created, resolves status=none -> tier=free",
    "after": "the repaired expression, same row, resolves status=active -> tier=pro",
    "why": "so the suite distinguishes REPAIRED from NEVER-BROKEN. A test that only shows the good state cannot tell those apart."
  },
  "deployment": {
    "backend": "dep-dakrge5bedkc73c8j8ig, api-pinned to de00f896, status live",
    "frontend": "dpl_FSZcecTxYuXCfb63dDs8RP8is7h8, READY, metaGithubCommitSha de00f896",
    "productionShaBefore": "2170a6ba6496d4673b54e68c2b738d89d7e4707f",
    "productionShaAfter": "de00f896f87037ffb550d35e8ef134ad02ef34e7",
    "applicationSourceDigestBefore": "84a025531a56da98adb48e1a0d44ad7afe43e824f482715f3041fb9a521a5e32",
    "applicationSourceDigestAfter": "6d3e86baef948711a4e464acba2a2742041a078bfbd7be9ada0ef4c4c148890a",
    "migrations": 0,
    "schema": "1800000023000, 55/55, aheadOfBuild [] — unchanged",
    "renderAutoDeploy": "no / off — unchanged",
    "expert": "EXPERT_EXECUTION_ENABLED=true, ceilings 1 and USD 1.00 — untouched",
    "monitoring": "CONFIGURED, webhook, serverErrorsInWindow 0",
    "noindexNofollow": "x-robots-tag: noindex, nofollow",
    "unexpected5xx": "none"
  },
  "liveProductionProof": {
    "authorizationMechanism": "One 53-character cryptographically random EMPLOYER_PRO_PROMO_CODES value, set by per-key PUT (43 -> 44 keys, nothing else added, removed or changed; the three Expert keys byte-identical) and applied by deploying the identical artifact. Never printed and in no artifact.",
    "authorizedPath": {
      "register": "201, planCode pro, promoApplied true",
      "freshLogin": "JWT planCode pro, billingEntitlements.fullSafeScope true",
      "classify": "HTTP 201 — a real deterministic analysis: six applicabilityDecisions (30 CFR 56.14107(a), 56.15005, 29 CFR 1910.212(a)(1), 1910.28, 1926.501, 1926.300(b)(2)) and riskBand Critical"
    },
    "unauthorizedControlPath": {
      "register": "201 with planCode FREE and promoApplied false, despite the body asking for planCode pro",
      "freshLogin": "JWT planCode free, fullSafeScope false",
      "classify": "HTTP 402 PAID_SUBSCRIPTION_REQUIRED, entitlement fullSafeScope",
      "meaning": "A public caller cannot self-promote to the same entitlement the authorized path obtained, proven on the same deployment in the same minute."
    },
    "directProductionDatabaseWritesDuringTheProof": 0,
    "commercialCharges": 0,
    "platformAdminOperations": 0,
    "expertAnalyses": 0,
    "providerCalls": 0,
    "spendUsd": 0.0
  },
  "cleanup": {
    "promoRemoved": "DELETE env-var 204, then the identical artifact deployed again. The service record shows EMPLOYER_PRO_PROMO_CODES absent and the environment byte-identical to its §301 starting state (43 keys, 0 added, 0 removed, 0 changed).",
    "syntheticAccounts": "Both deleted through the product's own DELETE /auth/me (200 each). Fresh login afterwards 401 for both; the authorized account's token returns 401 from classify.",
    "rollingDeployWindow": {
      "whatHappened": "The read-back that was supposed to confirm the removal was taken immediately after the deploy reported status 'live', and it SUCCEEDED — the request reached an instance still holding the old environment and created a promoted account. A second attempt shortly afterwards correctly returned 400 'Invalid promo code' and created nothing.",
      "consequence": "One live pro account existed in production whose password had not been retained, so DELETE /auth/me was not available for it.",
      "howItWasResolved": "ONE direct production database transaction, reproducing exactly what DELETE /auth/me does: refresh tokens revoked, email anonymized to deleted-<id>@deleted.safety-insite.local, password hash randomized, planCode returned to free, subscriptionStatus to none, deletedAt set. Verified afterwards: zero live pro accounts on the test domain, and the only active entitlement_grant is the owner's own pre-existing founder grant.",
      "disclosure": "THIS IS A DEVIATION FROM §301's '0 direct production DB writes'. That requirement is stated for the LIVE PRODUCTION PROOF, and the proof itself used zero — every step of it went through HTTP. The write was CLEANUP of state created by a flawed read-back, and it is reported rather than absorbed.",
      "registeredAs": "OPS-2"
    },
    "residual": "Two soft-deleted, anonymized accounts and one soft-deleted anonymized orphan. No customer data. No entitlement. No inspection, observation, finding or analysis was created."
  }
}
