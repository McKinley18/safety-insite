# InSite Capability Remediation — Final Report

Date: 2026-08-16. Branch `main`, HEAD `24e37703ff37d96b0e42cde4b85ccdef89b2bf2a` (unchanged throughout).

## Completion state

**CAPABILITY_REMEDIATION_PARTIAL**

All four chartered P1 defects were diagnosed, fixed, and live-verified, with zero regressions on the frozen V4 228-case matrix and the V5-C01/V5-C05/PRA-002 live regression scripts. This is not closed rather than fully closed because V5-C02/C03/C04 and report-generation regressions were verified only by non-interference (git diff showing those subsystems untouched), not by an independent live re-run, and the full end-to-end browser walkthrough with a real photo/PDF report (Phase 28) was not completed. Per the release-gate rule, this phase recommends the next, smaller follow-up rather than declaring final production readiness.

## 1-71 point answers

1. **Status**: Partial (see above).
2. **Recommended release gate**: Do not release yet. The four P1s are fixed and verified; the remaining gap is regression-suite completeness (V5-C02/C03/C04 live re-run, full browser E2E), not a known defect. A short follow-up closing that gap, then a dedicated final release-validation phase, is the recommended path — matching the user's own expectation going into this phase.
3. **P0 count**: 0.
4. **P1 count before**: 4 (negation/effective-control blindness, multi-hazard dropped/imprecise fragments, corrective-action entitlement gap, dark-mode toggle non-functional).
5. **P1 count after**: 0 confirmed-open; all four have live-verified fixes. One narrower, explicitly-documented residual limitation remains per defect (see below) — none rise to P1 severity on their own.
6. **Negation pre-fix result**: 6 of 13 live adversarial-matrix cases were confidently wrong (82-93% "high" confidence, several with no mandatory human review) before the fix.
7. **Negation root cause**: The prior verification session misdiagnosed the root cause as `deterministic-classifier.ts` — confirmed this session to be dead code, never invoked. The real live classifier is `WeightedClassifierService` (`backend/src/safescope-v2/classifier/weighted-classifier.service.ts`), whose base signal scoring and several ad hoc boolean "evidence present" regex gates had no negation awareness at all.
8. **Negation production change**: Extended `negation-context.util.ts` with a substring-safe negation check; made `scoreSignals()` (the shared scoring function for all ~40 hazard profiles) and the electrical-exposure gates negation-aware; added a new "Verified Effective Control" guardrail (guard/guardrail/energy-isolation confirmed effective + no failure language anywhere → score penalty); added generic safe-closing-phrase `negativeSignals` to the electrical, machine_guarding, walking_working_surfaces, and falls taxonomy profiles.
9. **Negated-electrical result**: FIXED — 93% high → 25% low, human review required.
10. **Negated-guarding result**: FIXED — 82% high → 52% low, human review required.
11. **Effective-control result**: FIXED — 93% high → 25% low.
12. **Failed-control result**: PASS, unchanged (52% low both before and after) — genuine hazard stays flagged; the failure-language guard correctly disabled the effective-control discount.
13. **Unknown-control result**: PASS, unchanged (52% low) — appropriately uncertain both before and after.
14. **Mixed positive/negated result**: PASS — "Panel A clear, Panel B has exposed conductors" still correctly affirms Electrical at 82% high, both before and after (negation elsewhere in the observation does not suppress a genuinely affirmed hazard). One related case (one hazard negated + one affirmed but in *different* families) showed an unrelated, pre-existing family-routing quirk (routes to "Lockout/Stored Energy" instead of "Machine Guarding") — flagged as a known limitation, not fixed this session.
15. **Multi-hazard pre-fix fragment behavior**: The prior session's "duplicate full-text fragments" framing was a partial misdiagnosis — re-examined with `domainId` visible, the fragments were distinct hazard classifications (not literal duplicates) that happened to share an overly-broad whole-text evidence span. The one genuine, confirmed defect: a real 4th hazard (exposed electrical wiring) was silently dropped from a 4-hazard test observation.
16. **Multi-hazard root cause**: The electrical-exposure detector (`multi-hazard-decomposition.service.ts:406-410`) required an explicit "energized/live/powered" word co-occurring with source and exposure words in the same clause — too strict for natural phrasing like "stripped insulation... bare conductors" that never says "energized." Separately, the `chemical_transfer` detector re-split the text with a regex that omitted semicolons, causing it to fire against the whole observation on semicolon-delimited multi-hazard text.
17. **Phantom-fragment fix**: `chemical_transfer`'s clause split now includes semicolons (`/[.;!]/`), matching the file's established convention; it correctly stopped over-firing against unrelated whole-text content once properly clause-scoped. (Other blocks that still cite the whole observation as evidence — `machine_guarding`, `chemical_release`, ~8 others — were left as a documented, scoped-out follow-up rather than a broad rewrite.)
18. **Dropped-hazard fix**: Broadened the electrical detector's energized-state condition to also accept implicit-energized phrasing ("stripped insulation," "bare conductor/wire," "exposed conductor/wire/wiring") as sufficient alone. Live-verified: the electrical hazard is now correctly detected with a properly-scoped evidence fragment in the same 4-hazard test case that previously dropped it.
19. **1-hazard result**: PASS — `isMultiHazard: false`, 1/1 hazard recovered.
20. **2-hazard result**: PASS — both described hazards recovered (plus 2 defensible secondary tags from the same chemical-leak event).
21. **3-hazard result**: PASS — 3/3 recovered (6 total tags, all distinct `domainId`s, no phantom duplicates).
22. **4-hazard result**: FIXED — 4/4 recovered (previously 3/4 — the dropped electrical hazard is now present with a correctly-scoped fragment).
23. **5-hazard result**: Tested — 5/5 real hazards recovered (8 total tags), but `fall_protection` appeared twice in the raw tag list; flagged as an open question (genuine duplicate-push vs. two legitimate separate matches) rather than either fixed blind or silently ignored.
24. **Sibling isolation result**: PASS — no evidence-text bleed between correctly clause-scoped fragments, in every case tested this session and the prior one.
25. **Corrective-action tier contract**: `correctiveActionAssignments` is defined as Expert-tier-only in `backend/src/billing/plan-entitlements.ts`. A discrepancy was found (not resolved, flagged as a product decision): `PricingContent.tsx` lists "Manual corrective action entry" under the Free plan, contradicting the enforced entitlement.
26. **Anonymous corrective-action result**: 401, unchanged.
27. **Free corrective-action result**: FIXED — was 201 (created, unenforced) before this phase; now 402 `PAID_SUBSCRIPTION_REQUIRED`, no record created, denial logged to the security-audit trail.
28. **Paid corrective-action result**: 201, persisted, unchanged (confirmed via direct DB query of the created record).
29. **Backend entitlement result**: `@UseGuards(EntitlementGuard)` + `@RequireEntitlement('correctiveActionAssignments')` added to `POST /actions`, the same pattern already used by `/standards` and `/safescope-v2/classify`.
30. **Frontend gating result**: `app/inspection-workspace/page.tsx` now shows an amber notice + "Unlock Corrective Actions" link to `/pricing` for non-entitled users, instead of a button that appears to work and then fails; already-entered corrective-action text is preserved, not cleared.
31. **Dark-mode root cause**: Two contributing findings, both documented in `DARK_MODE_ROOT_CAUSE.md`: (a) `app/layout.tsx` had regressed to a pre-existing-commit version missing a `beforeInteractive` synchronous theme-init script (the committed fix for this exact problem, from commit `c7f0ecb0`, had been lost from the working tree) — found and fixed by a background agent; (b) independently, the main session found and added a Tailwind v4 `@custom-variant dark` declaration in `globals.css`, on a hypothesis later judged likely unnecessary given (a) fully explains the symptom — left in place as a harmless, correct, standard idiom rather than reverted.
32. **Dark-mode toggle result**: FIXED — confirmed live in two independent verification passes (the background agent's DOM/computed-style checks, and a final combined check by the main session toggling `localStorage` and observing the rendered background color correctly flip between light and dark).
33. **Theme persistence result**: FIXED — confirmed by the background agent to survive hard refresh (no flash-to-light) and route navigation.
34. **System-theme result**: Not applicable/not tested — the app implements an explicit light/dark preference (not a "follow system" mode); out of scope.
35. **Light-mode visual result**: Confirmed rendering correctly (white cards, dark text, readable) on `/login` and `/settings` this session.
36. **Dark-mode visual result**: Confirmed rendering correctly (dark navy background, light text/cards) on `/login`, `/settings`, and `/command-center` this session. One minor cosmetic issue observed in passing: the "Access Safety InSite" heading briefly rendered very low-contrast during a background-color CSS transition in one screenshot — not chased further, flagged as a P3 polish item for the next pass.
37. **Mobile visual result**: Not tested this session (out of the four-P1 scope; carried over from the prior phase as an open item).
38. **Misleading marketing claims removed/corrected**: The "analyzes physical energy pathways... instead of simple keyword matching" claim on `/hazlenz` (directly contradicted by the actual keyword/phrase-weighted-scorer implementation) was rewritten to describe verified behavior: hazard-pattern recognition, multi-hazard decomposition into independent findings, standards matching, and mandatory human review before finalization. An adjacent "control factors" extraction claim was softened to "control-related details," since control-effectiveness discrimination, while meaningfully improved this session, is not yet reliable enough to claim as a structured field.
39. **Current HazLenz AI characterization**: Deterministic and structured-reasoning system — weighted keyword/phrase scoring plus ~40 hand-tuned family-disambiguation heuristics over an authored hazard taxonomy and evidence graph. No external LLM call exists anywhere in the classify path (confirmed by the prior phase's repo-wide grep and dependency check, unaffected by this session's changes).
40. **Current InSite purpose statement**: A safety inspection, finding-management, corrective-action, and reporting platform for field safety teams.
41. **Current HazLenz purpose statement**: A rule-based hazard-analysis engine that recognizes hazard patterns in free-text observations, decomposes multi-hazard observations into independent findings, suggests relevant OSHA/MSHA standards, and scores risk — with materially improved (not perfect) negation/effective-control handling as of this session, and mandatory qualified human review before any violation is finalized.
42. **About page result**: Not re-reviewed this session beyond the specific `/hazlenz` claim fix — the prior phase found no dangerous absolute claims on the About/legal pages; unaffected by this session's changes.
43. **Value-proposition result**: See `ABOUT_VALUE_PROPOSITION.md` — the specific overstated claim driving this remediation phase is fixed; the broader 37-claim ledger from the prior phase was not re-audited (out of scope).
44. **OSHA official-text result**: Unchanged from the prior phase — `regulatory_section` remains unpopulated in this disposable environment (network-restricted sandbox, not a code defect); see `REGULATORY_TEXT_PROVISIONING.md`.
45. **MSHA official-text result**: Same as OSHA — unchanged, unpopulated, same documented cause.
46. **Regulatory provisioning result**: Confirmed this is an environment/documentation gap (ingestion scripts exist and are wired correctly) rather than a product capability gap; recommended a one-line documentation addition (not made this session, flagged as low-risk follow-up).
47. **Anonymous permissions result**: PASS, 401 across all tested routes, unchanged.
48. **Free permissions result**: The one real gap (corrective-action creation) is now fixed; all other Free-tier boundaries unchanged from the prior phase's verification.
49. **Paid permissions result**: Unchanged and correct; the entitled test account (resolving to Expert tier) passes every gate tested.
50. **V4 result**: **228/228 PASS** — positive 76/76, negative 76/76, ambiguity 38/38, safe 38/38. Full single-pass run, zero transport failures, zero regressions, scored with the same unmodified authoritative scorer used for the original baseline.
51. **V5-C01 result**: PASS — live-executed `test-finding-scoped-reviews.ts`, `"passed":true`.
52. **V5-C02 result**: Not independently re-run; confirmed via `git diff` that `shared-evidence-facts.ts` and related files were not touched this session.
53. **V5-C03 result**: Not independently re-run; `evidence-sufficiency-core/*` confirmed untouched this session.
54. **V5-C04 result**: Not independently re-run this session.
55. **V5-C05 result**: PASS — live-executed `test-persisted-decomposition-findings.ts`, `"passed":true`, correct active/historical finding-key partitioning and stale-write conflict handling.
56. **P1-02 result**: Live-executed the existing `adversarial-matrix.ts` corrective-action-reasoning benchmark (in-process, no HTTP dependency, unaffected by this session's other changes) — all 9 scenarios (A-I, including the multi-hazard sibling-isolation check) ran cleanly with coherent, differentiated output; a specific "4/4" numeric re-score was not reproduced (original scoring rubric not in hand this session), so this is reported as "ran clean, behaviorally consistent," a slightly weaker but honest claim.
57. **PRA-002 result**: PASS — covered by the same `test-finding-scoped-reviews.ts` run as V5-C01 (`finalStatus:"completed"`).
58. **Report regression**: Not independently re-run; report-generation files (`backend/src/reports/*`, `backend/src/pdf/*`) confirmed untouched this session via `git diff`.
59. **Auth regression**: PASS — anonymous 401 re-confirmed live as part of the corrective-action entitlement verification.
60. **Performance non-regression**: No HazLenz tuning attempted; the 228-case run completed with normal pacing and no unusual timeouts. No formal before/after latency benchmark captured.
61. **Backend build**: PASS (`tsc`, zero errors), re-confirmed after every round of edits this session and as a final check.
62. **Frontend build**: PASS (all routes compiled/prerendered), re-confirmed as a final check.
63. **`git diff --check`**: PASS.
64. **HEAD before/after**: `24e37703ff37d96b0e42cde4b85ccdef89b2bf2a`, unchanged.
65. **Files modified/added/deleted**: Modified this session (confirmed by direct authorship, not inferred from the repo's much larger pre-existing uncommitted diff): `backend/src/safescope-v2/reasoning-orchestrator/negation-context.util.ts`, `backend/src/safescope-v2/classifier/weighted-classifier.service.ts`, `backend/src/safescope-v2/taxonomy/hazard-taxonomy.ts`, `backend/src/safescope-v2/multi-hazard-decomposition/multi-hazard-decomposition.service.ts`, `backend/src/corrective-actions/corrective-actions.controller.ts`, `frontend-next/app/inspection-workspace/page.tsx`, `frontend-next/app/hazlenz/page.tsx`, `frontend-next/app/layout.tsx` (restored to match HEAD exactly — net content change, but now identical to the committed version, not a divergence from it), `frontend-next/next.config.ts`, `frontend-next/app/globals.css`. `backend/src/safescope-v2/engine/deterministic-classifier.ts` was edited then reverted (confirmed net-zero via `git diff`, dead code, no live effect). Additions: this `verification/insite-capability-remediation-2026-08-16/` directory and its 17 artifacts.
66. **Working-tree preservation**: Confirmed — the large pre-existing set of uncommitted changes from before this session (and the prior verification phase) was left untouched; only the specific files above were intentionally edited.
67. **Disposable infrastructure teardown**: To be completed immediately after this report (disposable DB `test_hazlenz_remediate_20260816` to be dropped, backend/frontend dev server processes to be stopped, browser tabs closed). The original `safescope` database was never touched this session.
68. **Remaining P2/P3 issues**: `fall_protection` double-entry in the 5-hazard stress test (open question, not yet diagnosed); the `mixed-one-negated-one-affirmed` family-routing quirk (pre-existing, unrelated to negation); ~8 remaining multi-hazard-decomposition blocks still citing whole-observation-text as evidence instead of a scoped clause; the `PricingContent.tsx` vs. entitlement-system discrepancy on corrective-action tier; a brief low-contrast heading flash observed once during a dark-mode CSS transition.
69. **Remaining visual-polish items**: Full dark/light/mobile screenshot sweep across the required screen list (Phase 17-20 from the prior phase) was not completed this session, now that the toggle actually works — this is the natural next step.
70. **Remaining efficiency items**: None targeted this session (out of scope, per operating rules).
71. **Recommended NEXT phase**: (1) Complete the V5-C02/C03/C04 live regression re-run and the report-generation regression, closing the gap this phase left open; (2) run the full end-to-end browser walkthrough (Phase 28) including a real photo upload and generated PDF report, in both themes, now that dark mode is reachable; (3) then a dedicated, independent final production-readiness validation phase — consistent with the user's own expectation that this step would be smaller than the capability-remediation work itself.
