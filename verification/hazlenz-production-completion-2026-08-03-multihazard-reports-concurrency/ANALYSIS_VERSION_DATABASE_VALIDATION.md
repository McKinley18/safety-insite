# Analysis database validation

Read-only disposable query after version tests: the tested observation had 2 analysis rows and exactly 1 `current` row. The earlier browser-created observations each had one current row. No original development data was modified.
