# Next Codex prompt

Continue in `/Users/mckinley/Desktop/Safety_InSite` with no commit/push and no original-database mutation. Use a fresh disposable database and real Chromium.

The previous phase proved three UI decomposition/reanalysis smokes and two-context analysis 409/idempotent replay, but did **not** close the canonical persisted multi-hazard lifecycle. Build a browser fixture through the real canonical UI (`/inspection-workspace`) that creates a persisted site/inspection/observation, uploads PNG evidence, runs analysis, persists split findings, and exposes current/prior analysis history. Then complete all three scenarios (electrical+fall, guarding+energy, hot-work+compressed-gas) through per-hazard review, actions, tasks, finalization, report versions, PDF grouping/checksum/history, and authorization.

Implement and test any missing canonical workspace transitions. Expand the two-context browser matrix to stale/replay risk, actions, tasks, finalization, and reports. Query audit records for every transition and add missing audit events. Exit only when the phase criteria in the user prompt pass; otherwise update the lifecycle artifacts and blockers.
