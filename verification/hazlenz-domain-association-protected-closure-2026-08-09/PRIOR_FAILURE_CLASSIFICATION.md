# Prior broad-validation failures

The file-persistence errors targeting `/Users/mckinley/Desktop/safescope-data/persistence/audit_records.json` are `UNRELATED_PRE_EXISTING_FAILURE` / `HARNESS_ENVIRONMENT_FAILURE`: they arise in file-backed review/ingestion validators outside the domain-association path and do not alter classify output.

The later candidate-console assertion failure is also `UNRELATED_PRE_EXISTING_FAILURE` in that broad validator. It is not evidence of a cross-family association regression. Focused association, decomposition, causal-chain, output, and corrective-action strategy validators pass independently.
