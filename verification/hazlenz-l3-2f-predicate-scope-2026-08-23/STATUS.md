# L3-2f — status

> ## `L3_2F_PARTIAL — SEMANTIC_REASONING_QUALITY_GATE_NOT_YET_PASSED`
> ## `L3_2E_SCOPE_CONTRADICTION` — **CLOSED**
> ## `CURRENT_LEVEL1_ENGINE_REMAINS_CUSTOMER_AUTHORITATIVE`

Baseline HEAD `1feda622`, unchanged. Blueprint §36 carries the full record.

## What closed

The **structural face of the closed-vocabulary pattern**. F1, F2 and F3 were three functions asking
three versions of one wrong question — *is this token in my list?* — and `word-classes.ts` answers all
three from bounded structure instead. The argument that makes it hold: **function words are a
genuinely closed class, so completing them is complete; lexical verbs are open, but FINITENESS is
decidable from regular morphology plus the closed irregular-past inventory.**

On the sealed set the four predicate-scope cohorts scored **21 of 21 with zero binder deletions**.
`D-NG-04` is recovered end to end, RC-08's negated list still crosses its comma, and
**`noise_exposure` is sealed-validated with its exact label**, completing family coverage at
**24 of 24** for the first time in the programme.

`F4` was the mirror image and closed with both of its guards intact.

## What did not

Four high-consequence misses against a gate of zero — one quotation-fidelity failure, one **binder**
deletion from `fixed` sitting in a rejection vocabulary (the eighth instance of §32.5, with a known
treatment), and two provider-stage control-adequacy residues.

And the finding that outranks all four: **§36.7**. Two prompt variants, identical text, only its
position changed, trade high-consequence recall against clarification precision — 2 misses at 88.9%
precision, or 4 misses at 100%. **Neither satisfies both gates.** Four phases have now moved this
balance with prose; this is the first time both poles were measured against each other with
everything else held constant.

`L3_2F_ARCHITECTURE_REASSESSMENT_REQUIRED` was **assessed and not declared** — no genuinely new
semantic mechanism appeared. §36.7 is architecture-relevant evidence of a different kind, and it is
recorded rather than concealed.

## Read in this order

1. `ROOT_CAUSE.md` — F1–F6 proven against unpatched code, before any edit.
2. `HOLDOUT_FREEZE.txt` — the sixth set, frozen before the repair code.
3. `FINAL_STATE.txt` — the advancement gate, item by item.
4. `NEXT_ACTION.md` — what remains, and why the next phase must not start with a wording attempt.
5. `REPRODUCTION_COMMANDS.md`, `preservation-evidence.txt`, `SECURITY_AND_BOUNDARY.txt`.

**The current field corpus is exhausted.** There is no seventh stride.
