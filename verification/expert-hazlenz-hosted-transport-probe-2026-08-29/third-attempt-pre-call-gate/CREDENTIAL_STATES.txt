CREDENTIAL REACHABILITY — THIRD ATTEMPT (2026-08-30)
Phase 1 permits PRESENT / ABSENT only. No value, prefix, suffix, length, hash or encoded
form of any credential was read, printed, persisted or derived.

location                                                  ANTHROPIC_API_KEY
--------------------------------------------------------  -----------------
process environment (this session)                         ABSENT
fresh login shell (zsh -lc)                                ABSENT
backend/.env  <- the ONLY file the probe loads             ABSENT
.env, .env.local                                           ABSENT
~/.zshrc, ~/.zprofile, ~/.profile                          ABSENT
~/.local/bin/env (sourced by .zshrc)                       ABSENT
~/.claude/settings.json env block                          ABSENT
.claude/settings.local.json env block                      ABSENT
.envrc / direnv                                            not present

CONTROL MEASUREMENT — the environment is NOT filtered:
OPENAI_API_KEY in process environment                      PRESENT
OPENAI_API_KEY in fresh login shell                        PRESENT

NEW FINDING vs the second attempt (blueprint section 103.1):
  103.1 attributed the absence to 'exported in a different terminal after this session
  started'. That explanation is now RULED OUT. A fresh login shell -- which reads the
  user's profile from scratch, exactly as a newly-opened terminal does -- also reports
  ABSENT. The credential is in no persistent location a new terminal would read.

  backend/.env mtime: 2026-08-16 23:08:53
  This authorization:  2026-08-29/30
  The file named by section 103.8 as the route in has not been written to. It carries
  19 keys, none of them Anthropic-, Claude-, model- or gateway-related.

PROBE SELF-GATE (executed, exit 3, zero cost):
  BLOCKED -- ANTHROPIC_API_KEY is not reachable. No call attempted, $0.00 spent.
