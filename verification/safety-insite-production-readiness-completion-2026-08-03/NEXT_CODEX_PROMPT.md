# Next Codex prompt

Continue production-readiness work in `/Users/mckinley/Desktop/Safety_InSite` without commit/push or original-database mutation. Use a fresh disposable PostgreSQL database, preserve dirty work, and stop services. Start with the open Critical/High register in `PRODUCTION_READINESS_GAP_REGISTER.md`.

1. Build a durable authenticated Chromium suite for the three multi-hazard workflows. Complete clarification, reanalysis, prior/current version UI, per-hazard risk/actions/tasks, finalization, reports, PDF grouping/checksums, historical mutation, and owner/foreign authorization. Query database invariants after every transition.
2. Use two authenticated browser contexts to prove stale 409 UI recovery and duplicate idempotency for clarification, risk, actions, tasks, finalization, and reports. Add regression tests for every discovered gap.
3. Query and complete audit coverage for all state-changing and denial events with tenant-scoped support access.
4. Reduce frontend lint by production feature groups. Prioritize hooks, accessibility, async safety, then replace `any` in authentication, inspection, HazLenz, standards, reports, evidence, and offline code. Do not weaken rules.
5. Define and test a truthful offline contract: draft persistence, explicit unsupported server mutations, reconnect/retry, duplicate prevention, stale conflicts, quota, and auth recovery.
6. Run keyboard/accessibility/mobile/theme matrix and fix critical/high defects.
7. Measure authenticated performance with sample sizes and proposed thresholds.
8. Obtain/validate live private S3-compatible storage, run dependency audit in networked CI, and complete backup/restore/rollback/monitoring evidence.
9. Package qualified regulatory review for all 129 pending records; keep Part 1904 and unsupported coal scope fail-closed.

Exit only when all locally testable Critical/High gates pass; otherwise update the gap register, final report, and this prompt with exact evidence and external dependencies.
