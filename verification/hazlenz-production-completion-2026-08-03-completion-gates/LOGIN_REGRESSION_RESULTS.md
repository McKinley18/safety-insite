# Login regression results

API-level registration, login, entitlement, free-user denial, expired-entitlement denial, and cross-user isolation remain passing. Clean production-build Chromium normal click login now passes with HTTP 201 and command-center navigation after the loopback CORS fix. Reload persistence, logout, Enter-key submission, and expired-session browser behavior remain unverified.
