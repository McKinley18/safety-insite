# Application stack results

Disposable backend ran on `127.0.0.1:4210` and frontend on `127.0.0.1:3001`, with `DEV_AUTH_BYPASS=false`. Backend startup and route registration succeeded. The original development database was not targeted.
