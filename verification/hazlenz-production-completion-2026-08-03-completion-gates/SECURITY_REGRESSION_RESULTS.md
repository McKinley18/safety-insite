# Security regression results

`DEV_AUTH_BYPASS=false` was used for backend and repeatability runs. API-level entitlement isolation and cross-user denial remain passing. No credentials or bypass headers were used as release evidence. Browser authorization matrix remains incomplete.
