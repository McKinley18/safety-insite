# Next implementation phase: authoritative knowledge hydration and review-case closure

Work in `/Users/mckinley/Desktop/Safety_InSite` on branch `main`. Do not commit, push, reset, clean, stash, modify the original development database, or overwrite unrelated dirty work. Preserve the five protected HazLenz files unless an independently proven root cause can only be fixed there; stop before touching them.

Read every artifact in `verification/hazlenz-evidence-intake-foundation-2026-07-29/`, especially both result JSON files and `REMAINING_FAILURES.md`.

Implement the smallest next phase with these objectives:

1. Build an audited, versioned offline standards release covering every predicate family currently emitted by the evidence foundation for supported MSHA, OSHA General Industry, and OSHA Construction jurisdictions.
2. Ingest only authoritative official text, store source URL, title, effective/retrieval dates, checksum, jurisdiction, citation aliases, and release version; never fabricate missing text.
3. Add deterministic integrity checks that reject missing, duplicate, aliased-to-wrong-family, stale, or checksum-invalid entries.
4. Adjudicate all 45 original and 13 holdout NEEDS REVIEW cases with independently reasoned expert criteria. Fix only systemic retrieval, applicability, ranking, question-closure, or output-grounding causes; do not patch scenario phrases.
5. Ensure every definitive citation hydrates from the approved release and every candidate is visibly labeled.
6. Preserve the structured evidence snapshot, contradiction policy, quick intake, safe-state behavior, and human-review requirement.
7. Add a fresh independent holdout and rerun the original, current holdout, focused evidence tests, offline tests, production builds, canonical persistence/report regressions, and authenticated browser workflow.

Stop rather than using arbitrary web text as authority. Produce exact source and release evidence. Unsupervised HazLenz remains NO-GO unless separately justified by comprehensive expert validation.
