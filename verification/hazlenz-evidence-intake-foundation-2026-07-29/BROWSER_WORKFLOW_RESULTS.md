# Authenticated browser workflow

## Result: PASS

The repository Playwright harness exercised the real production frontend and backend at a 390×844 mobile viewport with dark color scheme:

1. registered and logged in through the UI;
2. used an explicit backend test entitlement;
3. created a durable site and inspection;
4. uploaded a real PNG through the authenticated private-evidence route;
5. submitted a minimal observation to the real HazLenz endpoint;
6. rendered targeted clarification;
7. corrected an extracted fact and re-ran analysis;
8. persisted three immutable analysis snapshots;
9. completed human review, finding, corrective action, task, and report;
10. verified the report snapshot contained structured evidence facts;
11. reloaded and verified persistence;
12. logged out and verified route protection;
13. verified PostgreSQL counts directly.

Persisted result: 1 observation, 3 analyses, 1 review, 1 finding, 1 evidence object, and 1 report.

The in-app browser connector could not initialize (`Cannot redefine property: process`), so the repository's Playwright release harness was used. This did not reduce application-path coverage.
