# Threshold results

The precision holdout included 30 threshold cases. The current production path preserves uncertainty and clarification for missing or contradictory measurements and does not invent numeric values. Threshold precision remains an active engineering limitation where the existing classifier cannot expose a structured measurement predicate.
