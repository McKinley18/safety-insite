# AMENDMENT 2 — THE `G4` CONTRADICTION IS RECONCILED, AND THE GAP THAT PRODUCED IT IS CLOSED

> ## `L3_INDEPENDENT_EVIDENCE_PLAN_AMENDED_V2 — HOLDOUT_CONSTRUCTION_REAUTHORIZATION_REQUIRED`
> ## `PLAN_EXECUTABLE = TRUE` · `HOLDOUT_CONSTRUCTED_AND_FROZEN = FALSE` · `HOLDOUT_SPENT = FALSE`
> ## `CURRENT_LEVEL1_ENGINE_REMAINS_CUSTOMER_AUTHORITATIVE` · `PRODUCTION_PROVIDER_SELECTION_REMAINS_OPEN`

Baseline HEAD `a7b21a26`, unchanged, upstream 0/0. **Specification phase only: zero inference, zero
provider probes, zero credential access, $0.00.** No holdout, no freeze record, no builder output, no
scorer, no row selected. The three protected sources are byte-identical before and after. **`G1`…`G10`
untouched.**

---

## 1 — `D-E` — the `G4` denominator is `21`

**Amendment 1's frozen `G4` membership did not change.** It remains *"every authored row whose truth
state is non-`ACTIVE`"*:

| F1 | F2 | F3 | F4 | F5 | F6 | F8b | **total** |
|---|---|---|---|---|---|---|---|
| 4 | 4 | 3 | 3 | 3 | 3 | 1 | **21** |

**Closure holds:** the `ACTIVE`-truth complement is F7 (3) + F8a (1) = **4**, and `21 + 4 = 25` — the
frozen authored total. The partition is **disjoint and exhaustive**.

> ### `G4 DECLARED CARDINALITY: 18 → 21. NOTHING ELSE CHANGED.`

No family joined or left `G4`. **F6's membership is untouched.** No authored control, truth state, or
false-`ACTIVE` definition changed. `G1`…`G10`, every selection rule, both strides, the realism rule,
every contamination rule and every provider gate are unchanged.

**Preserved verbatim:** *an `ACTIVE` result on **F3** or **F6** is a `G4` false-`ACTIVE` failure.*
`G4` remains a **hard zero gate**.

**`18` was an arithmetic contradiction, not a competing membership rule.** Amendment 1 computed
`4+4+3+3+3+1 = 18`, silently omitting F6's 3, while the same sentence enumerated F6 and two further
clauses independently placed F6 inside `G4`. **Three of four clauses already agreed; only the
arithmetic disagreed.** Adopting `21` corrects Amendment 1 *to its own stated membership*.

---

## 2 — `D-F` — the derived-cardinality invariant `NEW, GENERAL`

> **For every frozen acceptance set whose membership is specified by an enumeration or a
> deterministic predicate and whose expected cardinality is declared, the membership and its
> cardinality must be independently derived and required to match exactly — before construction is
> authorized.**

**Not limited to `G4`.** It binds the authored total, every F1–F8 allocation, both `G3` denominators,
the `G4` denominator, the `G7` pole, both source allocations, the holdout cardinality, the
independent/authored split and both inventories.

Three rules give it teeth:

- **A declared number is never accepted because it appeared in a previous amendment.**
- **Gate memberships are derived from truth semantics and cross-checked against the declared
  per-family flag — a disagreement between the two is itself a contradiction**, not a preference.
- **Where a set and its complement are both declared, the closure check must hold.**

On failure: **STOP** with `INTERNAL_CONTRADICTION`. **No opportunistic repair of a second
contradiction**, and no adjusting members or controls to make a number fit (`D-72`).

---

## 3 — Phase 3 — declared versus independently derived `39 quantities · 39 MATCH · 0 MISMATCH`

Every gate-bearing count in Amendments 1 and 2 was re-derived from its frozen membership rule.
Full table: `analysis/DERIVED_CARDINALITY_TABLE.txt`.

| quantity | declared | derived |
|---|---|---|
| gauntlet / realism / seed physical rows | 150 · 117 · 100 | **match** |
| offsets `k` (D-A, D-B) | 0 · 3 | **match** |
| partition sizes | 38,38,37,37 · 30,29,29,29 | **match** |
| selected rows | 38 · 29 | **match** |
| physical / distinct inventory | 367 · 366 | **match** |
| ambiguity truth | 87 / 2 / 28 / 89 | **match** |
| F1–F8 allocation, authored total | 4,4,3,3,3,3,3,2 · 25 | **match** |
| holdout total · independent · share | 92 · 67 · 72.8% | **match** |
| **G3 authored** — semantic, flag, enumerated | **6** | **6 · 6 · 6** |
| **G7 pole** — semantic, flag, enumerated | **11** | **11 · 11 · 11** |
| **G4 denominator** — semantic, flag, enumerated | **21** | **21 · 21 · 21** |
| closure `G4 + ACTIVE = 25` | 25 | **match** |

**The superseded `18` is not reproducible from any frozen membership rule** — all three independent
derivations yield `21`. **No second contradiction was found.**

---

## 4 — Phase 4 — independent executability review, full re-run `84 / 84`

> **The Amendment-1 verdict of 50/50 was NOT inherited.** Every criterion was re-evaluated against
> the combined plan — base + Amendment 1 + Amendment 2 — and the review was **extended** with the
> Section-B derived-cardinality criteria that did not exist before.

| section | what it proves |
|---|---|
| **A — determinism, re-proved from scratch** | source hashes, row counts, key presence and distinctness, carrier presence, mechanical offset derivation, **two independent implementations selecting identical row sets**, partition sizes, disjoint-and-exhaustive reservations, the 367/366 inventory, mutual disjointness, and the 87/2/28 ambiguity truth |
| **B — `D-F` consistency (NEW)** | every allocation, the composition, and the **triple agreement** of semantic / flag / enumerated derivations for `G3`, `G4` and `G7`, plus the closure check and the proof that `18` is unreachable |
| **C — clause presence** | all Amendment-1 clauses **plus** Amendment 2's, including that **Amendment 1 is present and not erased** and its original `G4` line survives verbatim |

**Result: `84 / 84` independently executable.**
**(1)** every construction decision is predetermined before source selection — **YES**.
**(2)** every declared derived cardinality agrees with its membership — **YES**.

One criterion initially failed on a **curly-versus-straight apostrophe** in a checker needle. The
clause was verified present in the plan and the **checker** was generalized to normalize typographic
punctuation — the clause text was not altered to satisfy a check.

---

## 5 — Phase 5 — authored-control structural recheck

The Attempt-1 materialization is used as a **structural artifact only** and is **admitted to no
holdout**. Derived from the frozen family rules, never hard-coded:

**25 controls · `4+4+3+3+3+3+3+2 = 25` · G3 = 6 · G4 = 21 · G7 = 11 · closure 21 + 4 = 25.**

**No authored control was changed to make a count fit.** The controls are byte-identical to their
Attempt-1 materialization (`4237fc3b…`); only the **declared number** moved.

---

## 6 — Phase 6 — contamination and spend proof

| check | result |
|---|---|
| source rows selected into any holdout | **0** |
| source identifiers materialized | **0** |
| acceptance holdout files anywhere | **0** |
| acceptance scorer executed | **NO — none exists** |
| provider credential accessed · provider probe · inference · egress | **NO · NO · NO · NONE** |

**Reserved offsets — all available, none spent:** gauntlet `0`,`1`,`2`,`3`; realism `0`,`1`,`2`,`3`;
the entire `gauntlet.seed` reserve (100 physical / 99 distinct), **unopened**. **Nothing retired.**

Proved **without semantically inspecting any reserved observation** — counts, hashes and file
existence only.

**Attempt-1's freeze `f0e33f14…` is unchanged**, remains **historical evidence only**, and Amendment 2
makes it binding that it **may never be reused as Attempt 2's freeze identity and never be rewritten**.
**No new freeze was created by this phase** — 7 freeze records exist: 6 pre-existing plus Attempt 1's.

---

## 7 — What remains false

`HOLDOUT_CONSTRUCTED_AND_FROZEN = FALSE` · `HOLDOUT_SPENT = FALSE`. **`D-83`'s artifact-level
precondition remains UNSATISFIED — the holdout still does not exist.** Attempt 1 remains
**invalidated**; it is not rewritten as though it succeeded.

`D-79`…`D-87` are not rewritten. **`claude-sonnet-5` was not called or probed; `D-70` and `D-77` are
unchanged and untested here. There is no model performance result in this phase.**
