# Corrective-action/task linkage

The first completed inspection produced 3 corrective actions, each with a distinct `findingId`, and 3 tasks. Each task's `correctiveActionId` references the action for the same finding. The second and third inspections likewise produced 3 actions and 3 tasks each. Database foreign keys and the recorded IDs show no cross-finding association.
