# Application stack

The production frontend build was created with an explicit disposable local API base. The backend ran with `DEV_AUTH_BYPASS=false` against the isolated database. Both services started successfully.
