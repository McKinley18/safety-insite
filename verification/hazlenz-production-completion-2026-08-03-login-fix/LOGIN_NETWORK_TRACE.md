# Login network trace

Observed browser behavior included page and static asset requests but no successful local `/auth/login` request during normal submission. The browser bundle contains the remote API host. A clean local production-build trace with a local API base remains required; no bypass or disabled CORS was used.
