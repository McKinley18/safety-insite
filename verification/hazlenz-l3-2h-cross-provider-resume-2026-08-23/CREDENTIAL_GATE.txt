L3-2h RESUME — PHASE 2 CREDENTIAL GATE
generated 2026-08-24T00:41:33Z

METHOD. Inspected by variable NAME, PRESENCE, LENGTH and PREFIX CLASS only.
No credential value was read into the transcript, printed, logged, persisted,
hashed, or partially revealed. The only transmission of the value was to the
provider's own authentication endpoint, which the entry contract names as the
sole authorized destination.

== 1. environment variables (names only) ==
  process environment, filtered for api/key/token/secret/provider names:
    CLAUDE_CODE_MESSAGING_TOKEN   (Claude Code harness, not a model provider)
    GEMINI_MODEL                  (a MODEL NAME string, not a credential)
    OPENAI_API_KEY                (present - see 2)

  UNSET: ANTHROPIC_API_KEY, GOOGLE_API_KEY, GEMINI_API_KEY, MISTRAL_API_KEY,
         COHERE_API_KEY, GROQ_API_KEY, TOGETHER_API_KEY, AZURE_OPENAI_API_KEY,
         DEEPSEEK_API_KEY, XAI_API_KEY, FIREWORKS_API_KEY, AWS_BEDROCK_*,
         VERTEX_*, and both gateway variables.

== 2. OPENAI_API_KEY — PRESENT BUT NOT A CREDENTIAL ==
  present:            true
  length:             11
  prefix class:       sk-proj
  substring scan:     no literal placeholder word
  whitespace only:    false

  A genuine OpenAI project key is on the order of 160 characters. At length 11
  the value carries roughly four characters beyond its own "sk-proj" prefix and
  cannot encode a key. Section 38.1 already recorded this exact variable at this
  exact length and classified it as the placeholder section 31.1 documented.

  NEW EVIDENCE THIS RESUME ADDS — the inference is now a measurement:
    probe:  GET https://api.openai.com/v1/models
            Authorization: Bearer $OPENAI_API_KEY
    result: HTTP 401 Unauthorized
    body:   discarded, not written to any artifact
    data:   NO scenario, observation, evidence or corpus content was included.
            The request carried the credential and nothing else.

  Section 38.1 concluded "placeholder" from length class. The provider itself has
  now rejected it. The gate outcome is unchanged; its proof is stronger.

== 3. no other credential mechanism ==
  ~/.config/gcloud/application_default_credentials.json   ABSENT
  ~/.aws/credentials                                      ABSENT
  ~/.config/anthropic                                     ABSENT
  repository .env files (.env, .env.local, backend/.env, backend/.env.example):
      ZERO hosted model-provider key names. Contents are database, JWT,
      throttling, Stripe billing and Vercel OIDC variables only.
  shell profile export of a provider variable: exactly one, OPENAI_API_KEY,
      i.e. the stub in 2. No second source.
  reasoning-l3 declares only: L3_OLLAMA_ENDPOINT, L3_OLLAMA_MODEL,
      L3_OLLAMA_TIMEOUT_MS. No hosted-provider variable exists in the module.

== 4. no substitute comparator ==
  ollama /api/tags returns exactly ONE model:
      qwen3-coder:30b  digest 06c1097efce0  18556700761 bytes
  Identical to the digest section 38.1 pinned. No second local model has been
  pulled since.

  Even had one been pulled, the entry contract and section 38.1 forbid treating a
  second local model as a provider-independence test unless the blueprint
  establishes that it answers the question. It does not. The substitution is
  prohibited, not merely unavailable.

== 5. GATE RESULT ==
  AUTHORIZED INDEPENDENT SECOND-PROVIDER CREDENTIALS AVAILABLE: 0
  The entry contract requires exactly one. Phases 3 through 8 MUST NOT execute.

  TERMINAL: L3_2H_BLOCKED - SECOND_PROVIDER_CREDENTIAL_REQUIRED
