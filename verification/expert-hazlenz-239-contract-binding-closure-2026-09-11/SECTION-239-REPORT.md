# §239 — Final Contract Binding Closure and Successor Candidate Preparation

**0 provider calls. 0 database operations. No commit, no push, no tag, no deploy.**

Terminal: `EXPERT_HAZLENZ_SUCCESSOR_CANDIDATE_FROZEN — FINAL_FRESH_ACCEPTANCE_DESIGN_AUTHORIZATION_REQUIRED`

Successor candidate digest: `b22e43957625afe696b417253ac46031f62b763a52e52b3eb9464ace0a81676e`

## 1. What §238 is, and stays

The §238 verdict is **FAILED** and this slice does not rescore it. `SECTION-238-JUDGMENT.json` is
read to confirm its digest and is never rewritten. The conjunctive pass rule was valid: three of six
analyses were inadmissible, and a correct posture inside an output the contract refuses is still a
failure.

What §238 also showed, and what the four §237 capability options were not written for, is that the
targeted semantic distinction **was made correctly on six of six fresh cases** — zero unsafe
under-conservative decisions, zero over-conservative decisions, zero response uncertainties
incorrectly elevated, zero false cessation drivers. None of the four options was invoked. The
remaining demonstrated issue was contract admissibility, and one deterministic correction was
authorized for it.

The three refusals are treated as three different things, because they are:

| | classification | disposition |
|---|---|---|
| **B1** | contract-binding defect | **repaired** |
| **B2** | correct fail-closed refusal | **preserved, and given its own refusal code** |
| **C1** | correct fail-closed refusal | **preserved untouched** |

Neither B2 nor C1 was repaired to improve the historical score.

## 2. B1 — the defect, and the smallest correction that admits it

§237 bound every driver role to exactly one reference kind, and bound both UNRESOLVED roles to
declarations alone. The reasoning was that an unresolved fact is by definition not an established
condition. That is right about what a candidate *asserts* and wrong about what the governed
candidate vocabulary *contains*: `EXPERT_CONDITION_STATES` carries `INSUFFICIENT_EVIDENCE` and
`UNKNOWN`, which exist precisely so an analysis can name a condition it could not place. A candidate
in one of those states **is** an unresolved property, written in the vocabulary the contract itself
gave the model. §238 B1 wrote exactly that and was refused for a coherent statement the contract had
no member for.

One role gains one reference kind under one state condition:

```
UNRESOLVED_PROPERTY_CONTROLLING_CONTINUATION
  may bind to an UNRESOLVED_DECLARATION                                       (as in §237), or
  to a HAZARD_CANDIDATE whose own assertedConditionState is INSUFFICIENT_EVIDENCE or UNKNOWN.
```

The two admissible states are **selected from the governed enum by construction**, not retyped and
not renamed; the module refuses to load if the filter no longer returns exactly two of eight. The
consistency check asserts they agree with `L3_UNDECIDED_STATES` one layer down, so the agreement is
checked rather than coincidental.

Nothing else moved. No role, no reference kind, no posture, no field and no schema property was
added. The prompt and the wire schema both reduce back to §237 byte for byte, and through §237 back
to §233.

## 3. B2 — why the contradiction still refuses

The state condition is the whole separator. §238 B2 attached the same role to a candidate it had
itself marked `ACTIVE` — established, present, obtaining. A condition the analysis has *placed*
cannot simultaneously be the unresolved property that decides whether work continues, and both
labels are the model's own.

New code: `UNRESOLVED_DRIVER_ROLE_CONTRADICTS_CANDIDATE_STATE`. It exists as its own code so the B1
and B2 shapes can never again be counted as one defect. It does not reinterpret `ACTIVE` as
`UNKNOWN`, does not repair the state, and does not create a declaration on the model's behalf. It
fails closed on a candidate carrying no governed state at all, and the suite sweeps it across all
six settled states rather than patching the one §238 happened to produce, and on a repeated
candidate key carrying two different states, which would otherwise be the same contradiction through
a side door.

## 4. C1 — untouched

P8 is carried forward from §235 through §237 without a character changed. A posture that permits
work states no resumption dependency. The §239 fixtures exercise it through both resume lists and
both permitting postures on subjects §238 never saw.

## 5. Contract alignment, in both directions

The §235 bidirectional invariant is maintained and gains the direction B1 failed. **C8** asserts
that every reference kind the validator admits for a role is stated to the provider, and that every
carrier the instruction offers is admitted by the validator — an instruction that invites a
representation enforcement refuses is exactly the defect this slice repairs, and it is now
mechanically impossible to reintroduce. **C9** asserts the state condition is transmitted and that
its vocabulary is the governed one.

| | |
|---|---|
| transmitted rules | 12 → 13 (one replaced, one added, eleven byte-identical) |
| refusal codes | 7 → 8 (one added, none retired) |
| registry entries | 27 |
| citations restated | 1 (`DRIVER_ROLE_INCOMPATIBLE_WITH_REF_KIND`) |
| prompt bytes | 78,386 → 80,104 |
| schema bytes | 26,188 → 26,474 |

The instruction gains a short paragraph naming both admissible carriers and stating plainly that
contradicting yourself is what is inadmissible.

## 6. What was deliberately **not** broadened

`UNRESOLVED_RESPONSE_OR_FOLLOW_UP` remains bound to declarations alone. The authorization broadens
the continuation-controlling driver and names the two sources it may bind to; it does not extend
that allowance to the response role, and no model has been observed writing that shape. Broadening
it would be a speculative change to a safety contract on no evidence.

**This is disclosed, not hidden, and it is held as a test** (fixture R2), because an undisclosed
narrowness is how B1 happened. If a model writes it, the analysis is refused exactly as B1 was. It
is listed as limitation K6 and is a shape for the fresh acceptance to observe.

## 7. Local proofs — 336 passed, 0 failed

All twelve authorized proofs are discharged on **generalized synthetic fixtures**. No subject, fact
pattern or candidate key from §234, §236, §237 or §238 appears: no saw blade, no vehicle lift, no
medical oxygen reserve bank, no powder-spray hopper, no kiln. Each proof is built for the *shape*
rather than the observation that exposed it, and several are swept rather than sampled — the whole
unresolved vocabulary, all six settled states, all four postures for the STOP floor, both resume
lists under both permitting postures.

The checks have teeth: the suite removes a registry entry and asserts C1 fails, transmits an uncited
rule and asserts C3 fails, widens a binding the instruction does not offer and asserts C8 fails, and
narrows the broadened binding below its own declaration and asserts C8 fails again.

## 8. Regression

| | |
|---|---|
| §239 local suite | 336 passed, 0 failed |
| §237 scenario equivalence | **22 / 22 identical** admission and identical code set |
| §237 suite | 204 passed, 0 failed |
| §235 suite | 209 passed, 0 failed |
| §233 suite | 109 passed, 0 failed |
| §239 / §237 / §235 contract consistency | 9/9 · 8/8 · 6/6 |
| protected ladder | 17 / 17, 0 failed, 0 missing |
| production typecheck | PASS |
| protected composite identity | `37ce9eb8…`, unchanged |
| §233 / §235 / §237 digests | unchanged, matching the frozen §238 protocol |
| prior evidence packages | §233 §234 §235 §236 §237 §238 all verify from the tree |

No protected module was modified, so no stop-and-ask boundary was reached.

**One disclosure.** The posture stack lives under `scripts/` and is not in the production TypeScript
program, so §237 and §238 were never typechecked. A scope config was added and it surfaced **two
pre-existing annotation errors** in those frozen modules — a reference to a name that does not exist
in `expert-237-posture-contract.ts:193` and a misplaced `const` assertion in the §238 design module.
Both are erased at emit and neither has any runtime effect, which is why every §237 and §238 result
stands. They were **not repaired**: the modules are frozen §238 provenance and editing them would
invalidate evidence that currently verifies. They are allowed by name in the §239 scope check so
that a new error in the §239 stack fails loudly, and they are recorded here for the product owner.

## 9. Diagnostic replay of the spent §238 outputs

Corroboration only, and labelled as such in the artifact. §238 is spent evidence; spent evidence may
diagnose a defect and may never serve as the acceptance cohort for the successor it diagnosed. Not
one of the three defective outputs was repaired — the defective bytes are the instrument.

| case | §237 | §239 | code |
|---|---|---|---|
| A1 | admitted | admitted | — |
| A2 | admitted | admitted | — |
| **B1** | refused | **admitted** | — |
| **B2** | refused | **refused** | `UNRESOLVED_DRIVER_ROLE_CONTRADICTS_CANDIDATE_STATE` |
| **C1** | refused | **refused** | `RESUME_CONDITION_UNDER_PERMITTING_POSTURE` |
| C2 | admitted | admitted | — |

The rule §239 changed is the rule that refused B1, and B2 and C1 still refuse. This is not a rescore,
not an acceptance, and not evidence of any semantic behaviour.

## 10. Intermediate posture development — closed

* **§231** exposed missing authoritative posture.
* **§233** introduced first-class posture state.
* **§234** exposed contract/wire and semantic weaknesses.
* **§235** stabilized the contract and established a STOP floor.
* **§236** demonstrated stable transport and exposed redundant cessation representation plus
  response-uncertainty escalation.
* **§237** removed the redundant state and established driver roles.
* **§238** demonstrated correct raw posture discrimination across all six fresh cases while exposing
  one overly narrow deterministic binding. **§238 is not a semantic acceptance pass.** It is
  accurately characterized as evidence that the targeted semantic distinction was demonstrated while
  overall protocol acceptance failed on contract admission.
* **§239** broadened that one binding, preserved both fail-closed boundaries, and froze the
  successor candidate.

## 11. Known limitations, carried forward honestly

Semantic posture correctness is not mathematically guaranteed. `driverRole` remains model-authored
semantic judgment, and fixture R3 holds that limit as a test: a declaration that is a follow-up
question in substance, labelled controlling by the model, is **admitted**, because refuting the label
would mean reading prose. Exact-property identity remains a known contained limitation. Human review
remains part of the product safety boundary. Fail-closed contract rejection remains possible when
model state is internally contradictory, and §239 adds one more way for that to happen; a refused
analysis yields nothing to the user. The full list is K1–K8 in the freeze document.

**Local architecture closure is not proof of universal safety correctness, and nothing here claims
it is.**

## 12. Successor candidate

Frozen, not accepted. All eight preparation steps completed: complete local integrated regression,
governed identity verification, protected identity verification, contract consistency verification,
evidence integrity verification, behavioural equivalence for unaffected paths, successor candidate
digest generation, and this freeze.

What would accept it is **one genuinely fresh final Expert acceptance** evaluating HazLenz as a
complete product capability rather than posture alone — hazard recognition, safe and negated
conditions, unresolved-fact behaviour, exact property behaviour, immediate posture, controls,
corrective actions, clarification, regulatory grounding, authority containment, settlement,
recommendation consequence, structural contract reliability, human-review containment and restraint
— with consequence-weighted hard gates, no reuse of §231, §234, §236 or §238 cases, and a cohort
that is not constructed primarily from historical failure shapes. It is designed **after** this
freeze and preregistered **before** any provider call.

That acceptance is intended to be the product decision. An ordinary quality miss does not open
another remediation cycle; a failure is classified by consequence and containment and answered with
ACCEPT, ACCEPT WITH EXPLICIT CAPABILITY BOUNDARY, REMEDIATE A MATERIAL UNCONTAINED DEFECT, or HOLD
RELEASE.

No case is authored here and no call is authorized.
