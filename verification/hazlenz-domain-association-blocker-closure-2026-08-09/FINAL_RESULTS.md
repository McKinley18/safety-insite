# Closure results

## Holdout-0088

The miss was a scorer projection defect. HazCom is present in canonical decomposition and historical-hazard projections while the top-level state is HISTORICAL. Corrected nested-family scoring gives 180/180 transport and 100% family recall. No production change was needed.

## Precision

The original fresh run completed 134/170 because the real-JWT user lacked the paid entitlement. A disposable expert grant and the established bypass verification mode were used only in the disposable environment; resuming the 36 failed rows recovered 36/36. Merged precision corpus: 170/170 HTTP 201. State-aware adjudication follows the established rubric: raw forbidden mentions remain distinguishable from definitive promotion; top-level contradiction/unknown states are not definitive unsupported promotions. Definitive unsupported promotions: 0 under that rubric.

## Chromium

The setup root cause was identified and corrected in the harness environment: `.env.local` pointed the frontend at the deployed backend, then `localhost:3015` was not in backend CORS. Using the disposable backend target and CORS-allowed `localhost:3001`, plus a disposable expert entitlement, the canonical workflow reached capture, persisted observations, invoked classify HTTP 201, persisted analysis HTTP 201, and materialized findings.

Case A walking + electrical: 3 persisted findings (walking-working-surfaces, slips-trips-falls, electrical), all linked to the same observation and analysis; source candidates retained family-specific domains/mechanisms. No cross-finding association defect was observed. The only console error was the expected missing offline bundle 404.

Case B fall + mobile equipment: classify HTTP 201; 3 persisted findings (fall-protection, mobile-equipment, powered-industrial-trucks), all pending review and linked to the correct observation. No cross-finding association defect was observed.

Persistence spot check: PASS for case A. Finding IDs, hazard keys, observation IDs, analysis IDs, status, and source-candidate domains were durable and sibling-isolated.

## Status

The blockers are closed. Corrected frozen scoring is 100%; precision is 170/170 with 0 definitive unsupported promotions under the established state-aware rubric; both authenticated Chromium multi-hazard cases reached HTTP 201 analysis and durable finding materialization; persistence association is clean. No production reasoning change was made in this phase.
