# Single-scenario proof

The controlled persisted inspection scenario entered an unguarded operating conveyor observation, advanced with `Next`, remained on Step 2, exposed `Review with HazLenz AI`, and issued POST `/safescope-v2/classify` with HTTP 201. The returned page rendered `EVIDENCE USED`, `HAZARD CATEGORY`, standards review (`29 CFR 1910.212(a)(1)`), confidence, follow-up questions, and the finding builder. The complete raw capture is `BASELINE_TRACE.json` and `baseline-after-next.png`.
