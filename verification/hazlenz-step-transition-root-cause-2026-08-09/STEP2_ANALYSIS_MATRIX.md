# Step 2 / analysis matrix

Every row in `CHROMIUM_20_RESULTS.json` has `step2Reached=true`, `step2Stable=true`, `reviewVisible=true`, `analysisStatus=201`, and `analysisRendered=true`. The returned browser text includes the persisted finding-builder and structured HazLenz sections. This is a 20/20 matrix, not an API-only assertion.
