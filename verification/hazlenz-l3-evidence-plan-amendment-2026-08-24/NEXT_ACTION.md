# What happens next

> ## `L3_INDEPENDENT_EVIDENCE_PLAN_AMENDED — HOLDOUT_CONSTRUCTION_REAUTHORIZATION_REQUIRED`
> ## The plan is executable. **The holdout is still not built, and this phase did not build it.**

---

## Next prerequisite — separately authorize the construction phase `USER ACTION`

`D-A`…`D-D` are closed, and the Phase 7 review answered **YES**: two independent implementations
derive the same holdout without semantic discretion. **That authorizes nothing by itself.**

A **separately authorized construction phase** must execute *Amendment 1* in this order:

1. **`HOLDOUT_FREEZE.txt` first**, before any selection code runs — the three source hashes, the
   `D-A` and `D-B` rules with their derived offsets (`0` and `3`), the `D-D` allocation, and the
   `G3-DEN` predicates.
2. **The builder** — `S-5` drift guard, `CMP` sort, `i % 4 === k` selection, verbatim carriage, and
   **overlap enforced by a throw** across all eight surfaces in `D-D.6`.
3. **The 25 authored controls**, from the `D-D.3` family table **alone**, with the positive stride
   **unopened**.
4. **Materialise, hash, record** — 92 rows, 38 + 29 + 25.
5. **The acceptance scorer**, against `D-84`'s frozen `G1`…`G10` and the `G3-DEN` predicates, unit
   tested on **synthetic fixtures only** — never against the new holdout.

**Only after that** does the acceptance run become reachable, and it needs three further things that
this amendment does not supply:

- a valid **`ANTHROPIC_API_KEY`** for an organization under the Commercial Terms (`D-79`);
- the **execution-time provider/model identity gate** passing for exactly `claude-sonnet-5`;
- **explicit user authorization to spend the single-use holdout.**

> **The gates are frozen and the builder is not.** A construction phase that finds a gate
> inconvenient changes the **builder**, never the gate (`D-72`, `D-84`).

---

## If the construction phase finds a gap

**It must STOP, not choose.** *Amendment 1*'s governing principle is explicit: *"Where this amendment
leaves a choice, that is a defect in this amendment and the construction phase must STOP rather than
choose."* A residual ambiguity is a new amendment, decided by the user — not a judgment call made
inside the phase that builds the exam.

---

## Explicitly NOT done by this phase

No holdout · no `HOLDOUT_FREEZE.txt` · no builder · no scorer · no authored control · no row selected
or materialised · no observation text inspected · no source identity printed · `G1`–`G10` unchanged ·
prompt, schema, validator, binder and sanctioned input builder unchanged · no provider called · no
credential accessed · no production provider selected · L3-3 not begun · nothing committed, pushed,
merged, rebased, reset, deployed or stashed.
